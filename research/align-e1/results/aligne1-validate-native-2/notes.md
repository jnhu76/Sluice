# aligne1-validate-native-2 — notes

INVALID attempt (retained immutable): harness bug 2 — perf output was
redirected to /dev/null (`-o /dev/null`), so no counters were captured;
48 runs, 48 gate errors. Bench itself ran correct (128 MiB, bench-side
gates green). Superseded by aligne1-validate-native-6.