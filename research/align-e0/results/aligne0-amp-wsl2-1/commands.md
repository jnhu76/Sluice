# aligne0-amp-wsl2-1 — session commands

See `environment.json` (git HEAD/branch/dirty, bench sha256) and `manifest.json` (matrix). Per-run invocation is in `raw/<tag>.perf.txt` (perf wrapper) and recorded as `cmd` in each record.
