// sluice::async::select — E13 Select entry point (P1 skeleton).
//
// P1: no production behavior. The select() function template is declared in
// the header but not defined here. This translation unit exists so that
// the build system glob (src/async/*.cpp) picks up the select component,
// and to provide an anchor for future implementation stages.
//
// P1 only: enums, type construction, compile-time constraint gates.
// See include/sluice/async/select.hpp for the public declarations.
