\* E10WaitNode TLC config (safety).
\* sluice-CORE-E10 — cancellation-safe WaitNode/WaitQueue protocol.
\*
\* Model: Nodes = {N0, N1}. Exhaustive over the 4-state node machine and the
\* wake||cancel race.
SPECIFICATION Spec
INVARIANT Inv

CONSTANTS
N0 = N0
N1 = N1
Nodes = {N0, N1}
