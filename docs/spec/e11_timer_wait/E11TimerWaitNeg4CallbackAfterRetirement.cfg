\* E11 NEG-4 negative-model TLC config.
\* Broken: node destroyed while a bound registration is still Active -> callback
\* dereference authority outlives the WaitNode storage (I4 violation).
\* Expected: InvTimerLifetimeClosure FAIL.
SPECIFICATION Spec
INVARIANT InvTimerLifetimeClosure

CONSTANTS
N0 = N0
N1 = N1
Nodes = {N0, N1}
R0 = R0
R1 = R1
Regs = {R0, R1}
