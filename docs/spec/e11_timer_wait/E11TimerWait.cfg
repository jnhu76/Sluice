\* E11TimerWait TLC config (safety).
\* sluice-CORE-E11 — Deadline / Timer Wait Integration (correct model).
\*
\* Model: Nodes = {N0, N1}, Regs = {R0, R1}, Time = 0..3, DeadlineVal = 0..3.
\* Exhaustive over the three-way RESOURCE_WAKE / TIMER_EXPIRE / CANCEL race on
\* the SAME resolve_ authority, the timer-registration lifetime (ACTIVE/RETIRED/
\* CONSUMED), and the deadline-due/park-liveness dimensions.
SPECIFICATION Spec
INVARIANT Inv

CONSTANTS
N0 = N0
N1 = N1
Nodes = {N0, N1}
R0 = R0
R1 = R1
Regs = {R0, R1}
