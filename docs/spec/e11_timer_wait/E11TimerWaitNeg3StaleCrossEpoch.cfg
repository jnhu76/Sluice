\* E11 NEG-3 negative-model TLC config.
\* Broken: timer keys on reusable storage slot, not logical epoch -> a stale
\* timer for epoch E resolves epoch E+1 when E+1 reuses E's slot.
\* Expected: InvSingleResolutionWinner FAIL (a slot is resolved twice).
SPECIFICATION Spec
INVARIANT InvSingleResolutionWinner

CONSTANTS
N0 = N0
N1 = N1
Nodes = {N0, N1}
R0 = R0
R1 = R1
Regs = {R0, R1}
A0 = A0
A1 = A1
Slots = {A0, A1}
