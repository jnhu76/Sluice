\* E11 NEG-5 negative-model TLC config (liveness).
\* Broken: Scheduler parks indefinitely past a due deadline (no park-timeout /
\* timer pump from the parked state) -> deadline lost, wait unresolved forever.
\* Expected: DeadlineParkLiveness FAIL (liveness counterexample).
SPECIFICATION Spec
PROPERTY DeadlineParkLiveness

CONSTANTS
N0 = N0
N1 = N1
Nodes = {N0, N1}
R0 = R0
R1 = R1
Regs = {R0, R1}
