\* PR #18 Event/Timer adapter non-vacuity witness: a timer account was closed (M premise).
\* A TLC "Invariant NotReachAdapterTimerAccountClosed is violated" on this cfg is the
\* witness that the premise is reachable in the adapter model, so the
\* corresponding safety law is not vacuously TRUE.
SPECIFICATION EventTimerSpec
INVARIANT NotReachAdapterTimerAccountClosed

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
