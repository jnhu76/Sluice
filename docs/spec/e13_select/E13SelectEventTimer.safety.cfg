\* PR #18 Event/Timer adapter safety suite: the named layered invariant set
\* (K + L + M + N) over the PR #17 Event/Timer adapter on a 2-arm domain.
\* PR #17 canonical EventTimerInv + Central refinement is checked separately by
\* E13SelectEventTimer.cfg and is NOT asserted here, so each suite has an
\* independent witness.  The adapter-only accounting counters and step-indexed
\* history are exercised by this suite.
SPECIFICATION EventTimerSpec
INVARIANT AdapterSafetyInv

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
