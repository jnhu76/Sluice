\* PR #18 Contract non-vacuity witness: suspended reservation completion.
\* A TLC "Invariant NotReachContractReservationSuspended is violated" on this cfg is the witness that the
\* premise is reachable in the Contract model, so the corresponding safety law
\* is not vacuously TRUE.
SPECIFICATION ContractSpec
INVARIANT NotReachContractReservationSuspended

CONSTANTS
MaxArms = 2
Arms = {0, 1}
