\* NEG-S7: add a member to the claim snapshot after claim.
\* Fault_S7 adds a snapshot-external registered arm j to claim_candidates
\* while leaving the frozen snapshot untouched.  Requires a 3-arm cfg so a
\* winner, a snapshot-internal candidate, and a snapshot-external registered
\* arm can coexist.
\* Target: S_InvClaimSnapshotImmutableAfterClaim.
SPECIFICATION SNegSpec
INVARIANT S_InvClaimSnapshotImmutableAfterClaim
CONSTANTS
MaxArms = 3
Arms = {0, 1, 2}
FAULT = "S7"
