\* E13Select scene: stale timer configuration (§L).
\* Proves R10 (stale Retired TimerRegistration skipped without dereference)
\* reachable (§K).
\*
\* The witness: a Timer arm whose registration is Retired (retired as a loser
\* via FinalizeTimerLoser: Active -> Retired, AFTER cancel/detach) persists to
\* group Completed/Destroyed. The pump's dereference gate (timer_state=Active
\* in ObserveTimerReady/TimerPumpEntry) means a Retired registration is never
\* dereferenced -- preparation §I / S15 / I29. NEG-T1 (the buggy variant that
\* DOES dereference) is PR #18; this scene proves the CORRECT model reaches a
\* state where a stale Retired registration coexists with a completed group.
SPECIFICATION Spec
INVARIANT NotReach_R10

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
