\* PR #18 Contract non-vacuity witness: Rollback phase reached (R9 premise).
\* A TLC "Invariant NotReachContractRollback is violated" on this cfg is the witness that the
\* premise is reachable in the Contract model, so the corresponding safety law
\* is not vacuously TRUE.
SPECIFICATION ContractSpec
INVARIANT NotReachContractRollback

CONSTANTS
MaxArms = 2
Arms = {0, 1}
