\* E13Select scene: 2-arm mixed Event + Timer configuration (§L).
\* Proves R5 (Event winner + Timer loser) and R6 (Timer winner + Event loser)
\* reachable (§K). The two arms race; whichever ClaimWinner selects wins and
\* the other is retired (Timer loser -> Retired reg + Cancelled; Event loser
\* -> Cancelled). The model explores both outcomes.
SPECIFICATION Spec
INVARIANT NotReach_R5

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
