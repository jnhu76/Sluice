\* E13Select scene: registration rollback configuration (§L).
\* Proves R9 (registration rollback after PARTIAL registration) reachable (§K).
\*
\* The domain is the registration rollback boundary only: rollback begins while
\* contract_phase = "Building" / central_phase = "Registering" / caller_state =
\* "Running" / winner = NoArm, before FinishRegistration and before any caller
\* suspension. Post-suspension cancellation is NOT modelled here.
\*
\* Witness path (2 arms): register a STRICT SUBSET of arms (registration_count
\* strictly between 0 and MaxArms), BeginRollback, then retire each registered
\* arm via the concrete non-publishing seam (RollbackEventArm / RollbackCancel-
\* Timer+RollbackRetireTimer), FinishRollback -> Aborted -> DestroyOperation.
\* R9 binds the witness to: rollback_started, a genuine partial subset (0 <
\* registration_count < MaxArms), every registered arm Retired with
\* arm_resolution = "Released" and authority closed, every unregistered arm
\* Detached with arm_resolution = "None", winner = NoArm, and zero result /
\* runnable / per-arm publication. The caller stays Running throughout; it is
\* never Waiting at Aborted/Destroyed. Mirrors the production T12a catchable-
\* exception rollback (test-plan §3 T12a).
SPECIFICATION Spec
INVARIANT NotReach_R9

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
