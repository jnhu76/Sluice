\* E13Select scene: registration rollback configuration (§L).
\* Proves R9 (registration rollback after partial registration) reachable (§K).
\*
\* The witness: BeginRollback after >=1 arm registered, RollbackRegisteredArm
\* retires the visible arm(s) via the non-publishing seam, FinishRollback ->
\* Destroyed. NO SelectResult published, NO runnable publication, winner=NoArm.
\* Mirrors the production T12a catchable-exception rollback (test-plan §3 T12a).
SPECIFICATION Spec
INVARIANT NotReach_R9

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
