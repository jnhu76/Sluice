\* PR #18 Contract safety suite: the full named layered invariant set
\* (H1-H5 + I) over the corrected PR #17 Contract, on a 2-arm domain.
\* PR #17 canonical ContractInv is checked separately by E13SelectContract.cfg
\* and is NOT asserted here, so each suite has an independent witness.
SPECIFICATION ContractSpec
INVARIANT ContractSafetyInv

CONSTANTS
MaxArms = 2
Arms = {0, 1}
