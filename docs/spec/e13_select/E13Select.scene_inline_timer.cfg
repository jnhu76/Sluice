\* E13Select scene: admission-ready inline Timer winner (§L inline config).
\* Proves R2 (Timer admission-ready inline winner) reachable (§K).
\* Timer is admission-ready iff timer_due at FinishRegistration (now>=2 after
\* a Tick, or deadline-due at registration). The model explores the Timer
\* inline path.
SPECIFICATION Spec
INVARIANT NotReach_R2

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
