\* PR #18 corrective-1 multi-group non-vacuity witness:
\* a real registration rollback started from the registration domain is
\* reachable (g_phase=Rollback, arm installed, caller Running, no winner).
\* A TLC "Invariant NotMG_ReachPreFinishRollback is violated" is the witness
\* that the premise of MG_InvRollbackRequiresRunningCaller /
\* RequiresNoWinner / NeverPublishes is reachable, so those rollback laws are
\* not vacuously TRUE.
SPECIFICATION MGSpec
INVARIANT NotMG_ReachPreFinishRollback

CONSTANTS
Groups = {0, 1}
Events = {0}
