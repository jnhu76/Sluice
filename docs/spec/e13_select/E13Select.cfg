\* E13Select main TLC config (safety/well-formedness).
\* sluice-CORE-E13 Select formal core (Event + independent Timer arm).
\*
\* PR #17 scope: this config checks the positive Contract coherence laws,
\* Central Claim strategy consistency, adapter well-formedness, and both
\* refinements. The complete PR #18 safety suite and NEG-1..7/NEG-T1 negative
\* models are deliberately not asserted here.
\*
\* Domain: a 4-arm bounded configuration (Arms = 0..3) -- exercises the full
\* arm-index domain (tie-break R7, same-Event multi-arm R8, mixed Event+Timer).
\* Arms are INTEGER indices because the winner tie-break selects the minimum
\* index (production arm_index : uint8_t; preparation S4.2/S7.3).
\* Smaller scene configs (2-arm, 3-arm, inline, suspended, rollback, stale-
\* timer, same-Event) live in their own .cfg files to keep each reachable
\* state space tractable.
SPECIFICATION Spec
INVARIANT Inv
CONSTRAINT FourArmMixedConstraint

CONSTANTS
MaxArms = 4
Arms = {0, 1, 2, 3}
Events = {0, 1}
