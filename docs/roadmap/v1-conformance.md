# Sluice v1 Conformance Ledger

**Target authority:** [v1 Architecture and Contract Reference](../explicit-io-v1-final-decision.md), revision v1-r3.
**Starting implementation baseline:** `c64f005e6e59e791f26a7ab4a594c33954f096dd`.
**Purpose:** track implementation and evidence, not add or relax contracts.

This ledger starts a new assessment. Previous architecture CLOSED/CONFORMANT
labels do not transfer. PR #389 changes documentation only; it does not implement
RequestCore, new Request APIs, progress integration, or shutdown behavior.

Formal evidence is governed by the
[v1 formal evidence map](v1-formal-evidence-map.md) (issue #391): existing
Lean/TLA+ assets are HISTORICAL / ADAPT / RETIRE_FROM_V1_EVIDENCE and none of
them is v1 evidence; future protocol and liveness closures must record their
evidence in the map's closure format. No row below changes status because an
old proof exists.

## Status vocabulary

| Status | Meaning |
|---|---|
| NOT_ASSESSED | No complete assessment against these target requirements recorded |
| GAP | A specific missing/nonconforming behavior has been identified with source evidence |
| IMPLEMENTED_UNVERIFIED | A candidate implementation exists; required evidence is incomplete |
| VERIFIED | Named requirements passed the recorded evidence for specified commits/configurations |
| DEFERRED_OUT_OF_SCOPE | Not a required v1 capability; no supported-product claim |

VERIFIED never means universal correctness. A required v1 item may not use
DEFERRED_OUT_OF_SCOPE. Split a row if only part has evidence. Keep safety,
conditional liveness, memory visibility and kernel evidence separately visible.

## Implementation assessment

| Slice | Root requirements | Status | Required evidence before closure |
|---|---|---|---|
| Shared File semantic oracle | SEM-02, SEM-03, SEM-04, ERR-01 | IMPLEMENTED_UNVERIFIED | See the A1 review record: direct execution and the canonical error table carry evidence; request-side ordering and effect reporting do not |
| Direct composition helpers (read_exact / write_all) | SEM-05, ERR-02 | VERIFIED | A2 review record: direct loops consume the shared composition rule; injected short/zero/error counts, confirmed prefix, structured stop reasons. The outcome publishes the failed attempt's effect certainty (`accounted` / `unknown`) from the shared rule, so ERR-02's distinction is observable on this path; the request-side representation stays owned by #400 |
| Minimal metadata and same-file identity | SEM-07 | VERIFIED | A2 review record: kind, size and explicitly available/unavailable identity on the direct path, no registry. Request-side file_info/size stays unassessed and owned by #400 |
| Explicit close and RAII error boundary | SEM-02, LIFE | VERIFIED | A2 review record: close consumes ownership on the first attempt including error; destructor and move-assignment fault injection |
| Direct invocation | INV, ARCH, W-01 | VERIFIED | A2 review record: W-01 explicit-close and RAII traces as a core-only consumer. Request-side invocation is unchanged and unassessed here |
| Admission and slot lifecycle | REQ, BOUND | NOT_ASSESSED | V04–V05, V08–V09, V24, V26; executable model and failure injection. Substrate half: see the B1-1 review record (standalone protocol substrate with model+test evidence; production request paths unchanged). Context-ownership half: see the B1-A review record (context-owned core, one context identity domain with an executed exhaustion boundary; production request paths still unmigrated). Production ThreadPool half: see the B1-B review record (ThreadPool acceptance/identity/terminal/publication/release/reclaim governed by the context-owned RequestCore); production io_uring half: see the B1-C review record (the same authority for io_uring); the adoption seam collapsed into the backend interface contract at B1-D (see the B1-D review record). Public-acceptance half: see the B2 review record (the accepted identity flows to a returned public Request on both backends; pre-accept failure returns no Request, post-accept failure keeps it owned) |
| Public Request and result lifetime | HANDLE, LIFE | NOT_ASSESSED | Move/consume/discard, retained results, release-build violation behavior. See the B2 review record for the public `Request<T>`/`RequestId` surface: consumption atomic with binding release, non-consuming observation, the empty/pending/published/consumed model, always-on destructor and context-lifetime diagnostics (release-build death evidence), retained-result capacity pinning, model consumption extension with killed mutants. Observer/progress/shutdown halves stay with #396/#397/#401; whole-row verdicts wait for independent final review |
| Observer attachment and retirement | OBS | NOT_ASSESSED | V06–V08; attach/publication and cancel/delivery interleavings |
| Progress and external integration | PROG, W-03 | NOT_ASSESSED | V10–V12, V23; both backends; no-busy-poll/no-lost-wake evidence |
| Public threading and memory handoff | THREAD, REQ-04 | NOT_ASSESSED | API concurrency matrix, publication review, deterministic handoff and race instrumentation |
| Cancel and effect reporting | CANCEL, ERR-02 | GAP | V13–V15, V19; unsupported/retryable/coalesced control behavior. A1 added reference rules but no request path represents an unaccounted remainder or preserves a count across a cancel |
| ThreadPool profile | BACKEND, PROD-02 | GAP | Full required operation matrix; bounded workers; shared conformance; shutdown. A1 recorded zero-length dispatch, precedence ordering and effect reporting as open |
| io_uring profile | BACKEND, PROD-02 | GAP | Request-side lifecycle/conformance gaps closed by B1-C (see the B1-C review record): shared conformance with zero recorded divergences, deterministic original-vs-control CQE ordering, stale-cookie anti-ABA, prepared/submitted accounting with poison convergence, real-kernel read/write/sync/zero-op/cancel/reuse. Still open for #400/#401: partial-submit kernel-visibility proof on hard submit failure, full cancel/effect matrix, durability profile, shutdown/poison state machine |
| Bounded pipeline scope | HOST-02, W-02 | NOT_ASSESSED | V18–V19; early return/exception cleanup and scope capacity |
| Shutdown and destruction | SHUT | NOT_ASSESSED | V20–V23; poison/retirement; retained-result and host-detachment checks |
| Core-only consumer build boundary | ARCH, MIG | VERIFIED | A2 review record: a standalone core-only consumer compiles, links and runs; no async definition reachable, no liburing linked |
| Installed-header / package surface | ARCH, MIG | NOT_ASSESSED | No install or package target exists in this commit, so no installed surface can be audited; the audit belongs to the legacy/retirement slice (#402) |
| Optional stackful host | HOST-01, HOST-03, W-04 | NOT_ASSESSED | If shipped as supported: task failure/stop/unwind, single-owner driver, bounded resources |
| Legacy and compatibility retirement | MIG-02 | NOT_ASSESSED | Consumer audit; replacements usable; old Completion/public surfaces explicitly retired |
| Windows/macOS, multi-worker host, coroutine adapter | PROD-02 | DEFERRED_OUT_OF_SCOPE | Root amendment before supported-v1 claims |
| Registered/vectored/direct-I/O/zero-copy extensions | PROD-03, LIFE-03 | DEFERRED_OUT_OF_SCOPE | Workload and root amendment; no inheritance from an existing header |

NOT_ASSESSED does not imply the baseline has no useful implementation or evidence.
It prevents partial or older evidence from silently becoming certification of
the new target. The implementing PR records the precise source/evidence.

A row moves off NOT_ASSESSED only through a recorded review below. A GAP row
means a specific missing/nonconforming behavior was identified with source evidence;
it does not mean the whole slice is unimplemented.

## Evidence record template

For each reviewed slice, record:

| Field | Required content |
|---|---|
| Requirement scope | Exact IDs and accepted workload; explicit exclusions |
| Implementation | Commit SHA, code paths and public API/configuration |
| Change | Current behavior, target behavior and compatibility impact |
| Semantic/regression evidence | Test names/commands, result and output artifact or durable CI link |
| Protocol/model evidence | Model-to-C++ mapping, explored bounds, safety/liveness properties and assumptions |
| Publication/thread evidence | Synchronization argument and relevant instrumentation/configuration |
| Backend/kernel evidence | Backend, compiler, kernel/liburing, filesystem, injected faults and limitations |
| Status | New status and remaining gaps; never close a required slice with skipped tests |

## Phase gates

Use MIG-01 for the normative sequence and VERIFY-04 for named counterexamples.
Implement evidence with the changed protocol. Phase B already includes shutdown
and observer-reference obligations even though full integration closes later.

1. A: semantic oracle and direct path.
2. B: core, handles, publication/reclaim and teardown skeleton.
3. C: host-neutral observation and persistent progress integration.
4. D: bounded scope and optional host cleanup.
5. E: required operations, cancellation, both backend profiles and shutdown closure.
6. F: compatibility/public-surface retirement after consumer migration.
7. G: measured optimization preserving verified obligations.

Release evidence must enumerate actual supported configurations and pass W-01,
W-02, W-03, and W-04 for any supported optional host. This file is intentionally
not a claim that those gates have already passed.

## A1 review record — Issue #392, shared File semantic oracle

This record uses two axes, and they must not be conflated:

- The **assessment table** above uses the ledger status vocabulary.
- The **gap map** below uses the A1 slice's own coverage vocabulary:
  `CONFORMING` (this slice verified that rule on that path with the cited
  evidence), `GAP` (a specific divergence, with an owner), `IMPLEMENTED_UNVERIFIED`
  (a candidate or reference exists, evidence incomplete), `OUT_OF_SCOPE`.

`OUT_OF_SCOPE` in the gap map means *this slice did not own that requirement*, not
that v1 does not require it. A required capability is never recorded as
DEFERRED_OUT_OF_SCOPE anywhere in this file; where a required capability is simply
not assessed yet, the gap map points at its assessment-table row instead.

| Field | Content |
|---|---|
| Requirement scope | SEM-02 open rules; SEM-03 access matrix and validation precedence steps 1–4; SEM-04 offset/length/range; SEM-05 primitive and composition rules; SEM-06 durability rules; ERR-01 canonical mapping; ERR-02 effect representation. Excludes SEM-01 operation matrix closure, SEM-07 metadata/identity, direct composition helpers, and every request-side responsibility |
| Implementation | `include/sluice/detail/file_semantics.hpp` is the single semantic authority. `src/file_resource.cpp`, `src/blocking_file.cpp`, `src/async/file.cpp`, `src/async/async_io_context.cpp`, `src/async/threadpool_backend.cpp` and `src/async/uring_backend.cpp` call the validation rules and the error mapping; none of them re-derives File legality. The composition, effect and durability rules have no production consumer in this commit |
| Change | `ENOENT`/`ENOTDIR` now map to `not_found` (was `permission_denied`). Embedded NUL is rejected before the native open (was silently truncated). An unrepresentable offset is `invalid_argument` (was `invalid_state` for the legacy reader). The last addressed byte is validated without overflow and a transfer above the native count is an invalid range (io_uring had no such check). `detail/io_validation.hpp` became `detail/uring_submit.hpp` because its remaining content is io_uring mechanism, not File validation. Review corrections: ECANCELED falls back to `backend_error` per ERR-01, since the root assigns it no canonical category and the semantic `canceled` outcome comes only from CANCEL-01 dispositions; buffer presence is no longer claimed as an SEM-03 rule and each raw-pointer surface keeps its own null-buffer fail-fast; effect certainty is derived from dispatch evidence rather than read/write direction; durability supersession requires strict ordering plus a conflicting mutation |
| Semantic/regression evidence | `semantic_errno_mapping_test` (14 table cases, table consistency, real `ENOENT`/`ENOTDIR`/`EACCES` failures), `semantic_open_test` (18-combination legality table, oracle-versus-real-open cross-check, NUL rejected pre-open), `semantic_range_test` (13 range cases cross-checked against wide-arithmetic restatement), `semantic_short_io_reference_test`, `semantic_effect_outcome_test`, `semantic_durability_reference_test`, `semantic_validation_precedence_test` (21 scenarios), `semantic_reference_case_test` (V01–V03, V15–V17, V27) |
| Protocol/model evidence | None claimed. A1 is a table/property slice; the request lifecycle model belongs to Phase B |
| Publication/thread evidence | None claimed. This slice does not change publication or threading |
| Backend/kernel evidence | Direct, ThreadPool and real-kernel io_uring (`--liburing=y`, 8-case smoke passing) are compared against the same scenario table. io_uring unavailability is reported NOT RUN, never as a pass. `/dev/full` is a fault-injection device outside the PROD-02 regular-file domain and is used only to produce a deterministic write failure |
| Status | Shared File semantic oracle: IMPLEMENTED_UNVERIFIED. Validation rules and the error mapping are called by every execution path; the composition, effect and durability rules are a reference model with no production consumer yet, and their tests prove properties of that model rather than of a path. Direct path: CONFORMING for the tested rules and scenarios below, except the zero-op no-OS-call half of SEM-03 step 3, which stays IMPLEMENTED_UNVERIFIED (inspection-only evidence) until #393; the direct scope as a whole is therefore not CONFORMING. Request paths: GAP, see the map. **[A2 update, later commit]** the composition rules now have a production consumer and the zero-op no-OS-call half is CONFORMING; see the A2 review record below. The effect and durability rules remain a reference model |

### A1 baseline gap map

Rule/scenario | Current path | Observed behavior | v1 expected | Status | Owner
---|---|---|---|---|---
Open combination legality, defaults, embedded NUL | `File::open` | `invalid_argument` before the native open; NUL no longer truncates the name | SEM-02 | CONFORMING | —
Access matrix and precedence steps 1–4 | `sluice::blocking::*` | All 21 drivable scenarios match the oracle | SEM-03, SEM-04 | CONFORMING | —
Offset/length range, last-byte overflow | canonical File, direct syscalls | Unrepresentable offset and an overflowing last byte are `invalid_argument`; above-native transfer is a pure rule with no drivable buffer | SEM-04 | CONFORMING | —
Native error classification | `from_errno_value` | Single table; `ENOENT`/`ENOTDIR`→`not_found`, `EACCES`/`EPERM`→`permission_denied`, ECANCELED deliberately unmapped→`backend_error`, native detail preserved | ERR-01 | CONFORMING | —
Primitive EOF / short transfer / zero-length | direct syscalls | Matches `classify_primitive` on real files; a scripted short write is returned as a short success and no further native call is made for the remaining bytes | SEM-05 | CONFORMING (A2) | —
Exact/all composition | direct | Direct `read_exact`/`write_all` (shared cursor and positional) loop over the shared composition rule and report the confirmed prefix next to a structured stop reason | SEM-05 | CONFORMING (A2) | —
`file_info` / size / same-file identity | canonical File | `file_info` reports kind, observed size and an explicitly available/unavailable identity; `size` is the projection of its own observation rather than a second metadata rule, with no snapshot across separate calls; comparison is value-level with an explicit unknown | SEM-07 | CONFORMING (direct, A2) | —
Explicit close, destructor, move-assignment error boundary | canonical File | First native close attempt consumes ownership including on error, no retry after EINTR, destructor is a noexcept single attempt, move assignment best-effort closes the old resource | SEM-02 | CONFORMING (A2) | —
Zero-length request | ThreadPool, io_uring | **[B1-B update, later commit]** ThreadPool: CONFORMING — the zero-op is accepted, published at acceptance (`ready()` on submit return) and never dispatched; measured by `zero_op_publishes_at_acceptance_without_dispatch`, and the recorded ThreadPool divergence set is now empty. io_uring: **[B1-C update, later commit]** CONFORMING — the same rule on the request path (`zero_op_publishes_at_acceptance_without_dispatch` in the uring cutover suite: ready at submit return, zero SQEs/cookies/ledger entries, the owed ready event pinned by a core control ref and discharged by the next poll with synchronous reclaim); the recorded uring divergence set is now empty (`semantic_backend_conformance_test`, `semantic_validation_precedence_test`) | SEM-03: never dispatches a data syscall | CONFORMING (ThreadPool, B1-B; io_uring, B1-C) | #394 closed for this row
Zero-length direct call | `sluice::blocking::*` | Returns 0 with zero intercepted native transfer calls under the test-only seam, on the primitives and on both composition surfaces | SEM-03: no OS call | CONFORMING (A2) | —
Precedence step 4 (range) vs step 7 (capacity) | both request backends | **[B1-B update, later commit]** ThreadPool: CONFORMING — validation runs before the core reservation, so a saturated table still reports `invalid_argument` for an invalid range (measured by `range_precedes_capacity_under_saturation`). io_uring: **[B1-C update, later commit]** CONFORMING — validation precedes the core reservation on the io_uring submit path; a saturated table still reports `invalid_argument` for an invalid range, and range validation runs before the core's admission gate, so the invalid range outranks a closed context and the closed context outranks capacity (measured by `range_precedes_health_and_capacity` in the uring cutover suite) | SEM-03 | CONFORMING (ThreadPool, B1-B; io_uring, B1-C) | #394 closed for this row
Precedence step 4 (range) vs steps 5–6 (health/support) | both request backends | **[B1-B update, later commit]** ThreadPool: CONFORMING — range validation precedes the core's admission gate, so an invalid range outranks a closed context and a closed context outranks capacity, both under the core's serialized authority (measured by the closed-precedence scenarios and `admission_close_between_reserve_and_accept_refuses_and_rolls_back`). io_uring: **[B1-C update, later commit]** CONFORMING — validation precedes the core reservation on the io_uring submit path; a saturated table still reports `invalid_argument` for an invalid range, and range validation runs before the core's admission gate, so the invalid range outranks a closed context and the closed context outranks capacity (measured by `range_precedes_health_and_capacity` in the uring cutover suite) | SEM-03 | CONFORMING (ThreadPool, B1-B; io_uring, B1-C) | #394 closed for this row
Partial effect on failure | both request backends | `TerminalResult::err()` writes `bytes = 0` and the terminal has no effect-certainty channel, so an error plus confirmed bytes or an unaccounted remainder is not representable | ERR-02, V15 | GAP | #400
Cancel racing a completed count | both request backends | `record_canceled` stores `err(canceled)` with no count, so a raced success is erasable | ERR-02, CANCEL-01 | GAP | #400
Zero-progress write code | `op_helpers`, `await_op_helpers` | Three different codes across composition helpers (`invalid_state`, `backend_error`) and no confirmed-byte reporting | SEM-05 | GAP | #400
Durability coverage of a completed resize | direct resize + `sync_data`/`sync_all` | Sequence succeeds and the size change is observable; no request-path evidence exists | SEM-06, V27 | IMPLEMENTED_UNVERIFIED | #400
Submitted-then-sync ordering | request paths | No API reports coverage, so V16 cannot be exercised end to end | SEM-06, V16 | IMPLEMENTED_UNVERIFIED | #400
EINTR retry | direct syscalls | A scripted `EINTR` attempt with no completed count is retried and the following count is returned, in both directions; a successful short count ends the primitive, so on these direct surfaces further calls for the remainder come only from the exact/all composition; `close` is never retried, and its `EINTR` consumes ownership | SEM-05 | CONFORMING (A2) | —
Undisclosed transfer limit | io_uring | A request longer than the SQE count is lowered to a short count without disclosing the limit | BACKEND-02 capability disclosure | GAP | #400
Ad-hoc errno classification outside the table | io_uring submit path, io_uring wait source | `EINTR`/`EAGAIN`/`EBUSY` compared locally next to shared classification | ERR-01 | GAP | #400
Closed-operation error | legacy `Reader`/`Writer`/`FileReader`/`FileWriter` | Fabricates `permission_denied` (no native detail) where the oracle requires `invalid_state` | SEM-03 step 1 | GAP | #402
Access legality, composition codes, confirmed bytes | legacy reader/writer, buffered layer, copy | No canonical access check; `read_exact`/`write_all` return no confirmed prefix and stop with locally chosen codes | SEM-03, SEM-05 | GAP | #402
Closed descriptor error code, copied open flags | `src/experimental/*` | Returns `permission_denied`; duplicates the writer flag lowering; in no build target | SEM-02, SEM-03 | OUT_OF_SCOPE | #402
Unused submit-classification helpers | `detail/uring_submit.hpp` | `classify_uring_submit`/`UringSubmitProgress` have no call site | — | OUT_OF_SCOPE | #402
Code-to-name table for CLI output | `apps/sluice-copy/cli_parse.cpp` | Second presentation table with a `default` fallback; not a semantic authority | — | OUT_OF_SCOPE | App surface
Task exception to terminal error | `include/sluice/async/task_result.hpp` | A `std::system_error` now routes through the shared mapping, so an `ENOENT` there is `not_found`. `std::bad_alloc` still becomes `no_space`, which ERR-01 does not assign; recorded as an open item below | ERR-01 | CONFORMING (mapping) / GAP (`bad_alloc` category) | #399
Fabricated not-found substitute | `include/sluice/memory_io_context.hpp` | A seeded-path miss returns `permission_denied` with no native detail | ERR-01 | GAP | #402

### A1 open items recorded rather than decided

- SEM-05 requires composition to stop with a "no-progress failure" without naming
  a canonical category. The oracle reuses `invalid_state` as the closest existing
  category and adds no new category. The root does not specify this mapping, so
  it is recorded here as an ambiguity rather than treated as settled.
  **[A2 update, later commit]** that reuse is a projection of the canonical stop
  state rather than the composition authority; the public direct outcome does not
  adopt it. See the A2 review record.
- Buffer presence is not an SEM-03 rule: SEM-03 treats caller memory validity as
  not generally dynamically detectable. Each raw-pointer surface keeps a
  null-buffer-with-nonzero-length fail-fast as its own implementation
  precondition, outside the shared contract; a surface whose interface takes
  spans does not need one, and native-to-semantic refinement of such cases
  belongs to #400.
- An allocation failure (`std::bad_alloc`) reaching the host boundary has no
  canonical category. `no_space` is used as the closest existing one, but ERR-01
  describes `no_space` for the filesystem and no requirement assigns this case,
  so the mapping is recorded rather than treated as settled.
- The oracle rejects only what is not natively representable. A filesystem may
  still refuse an accepted offset near the native maximum; that refusal is an
  operation result with native detail, not a validation rejection.
- V16 and the request-side half of V15 and V27 need request-path capability
  (an unobserved write followed by a sync, and a terminal that can carry an
  unaccounted remainder). Their rules are asserted; their production evidence is
  owned by #400 and is not claimed here.

## A2 review record — Issue #393, direct File closure and W-01

This record uses the assessment table's status vocabulary for the rows it
updates, and the A1 gap-map vocabulary for the A1 rows it closes.

| Field | Content |
|---|---|
| Requirement scope | SEM-01 direct column (open/close, positional and shared-cursor I/O, sync, file_info/size, resize, read_exact/write_all); SEM-02 close/RAII/ownership; SEM-04 cursor and offset rules; SEM-05 composition; SEM-06 durability as a direct integration sequence; SEM-07 metadata and identity; ERR-02 confirmed progress and effect certainty (`CompositionOutcome::remaining`, decided by the shared rule); INV-01 direct selection; ARCH-02 File ownership; W-01; VERIFY-01 build/public boundary (V25); VERIFY-04 V01–V03 and V27 direct halves. Excludes every request-side responsibility: request `file_info`/`size`/`resize`/composition forms, request effect-certainty representation, backends, RequestCore, observer, progress, cancellation, shutdown |
| Implementation | `include/sluice/file_resource.hpp` (`FileKind`, `FileIdentity`, `FileInfo`, `IdentityMatch`, `identity_match`, close/RAII contract comments); `include/sluice/blocking/file.hpp` (`file_info`, `CompositionEnd`, `EffectCertainty`, `CompositionOutcome`, `read_exact(_at)`, `write_all(_at)`); `src/blocking_file.cpp` (one `fstat` metadata observation with `size` as the projection of its own observation; the composition loop consuming `detail::compose_progress`/`compose_error` and publishing `detail::composition_effect_certainty`); `src/file_resource.cpp` (`close` routed through the test-only native-call seam); `src/file_test_seams.hpp` (bounded allocation-free native-call script, family- and descriptor-filtered, compiled only under `SLUICE_FILE_INTERNAL_TESTING`); `xmake/tests.lua` (four core-only targets and two seam targets that compile their own copies of the direct TUs and link no other core object); `src/file.cpp` only follows the seam's new signature at its legacy `FileReader`/`FileWriter` call site, and no target compiles that TU with the macro, so those legacy close paths remain un-fault-injected |
| Change | Direct path gains minimal metadata/identity and the exact/all composition surfaces. The composition outcome also gains the ERR-02 effect certainty (`CompositionOutcome::remaining`), which the direct path previously left unrepresented, so a failed attempt's possibly-effective remainder is reportable instead of being claimed away. `File::close` already consumed ownership on the first attempt before its native call; the change routes that call through the seam so the rule is fault-injectable and unchanged in behavior. No public behavior was removed; the new `size` is the projection of `file_info`, so the previous `size` behavior is preserved. Legacy `Reader`/`Writer`/`FileReader`/`FileWriter` composition codes are untouched and stay with #402 |
| Semantic/regression evidence | `file_close_semantics_test` (8 cases: unarmed release, success, EINTR, other error, destructor, move assignment, descriptor-reuse, closed no-op), `direct_composition_fault_test` (14 cases: short reads/writes, EOF-before-full prefix, zero-progress stop after one attempt, error-after-prefix with `no_space`/ENOSPC, a scripted short primitive write returned as a short success with one intercepted call carrying the original buffer, count and offset, primitive EINTR retry in both directions — an interrupted attempt with no completed count is repeated and the following count is returned, and the first successful short count ends the primitive, so the remaining bytes are not retried by it — zero-length with zero intercepted calls, impossible count, buffer/offset advance per confirmed bytes recorded at the native-call boundary, and the five canonical stop reasons pinned side by side with the reference `IoError` projection (`detail::composition_error`), which also pins effect certainty at every stop, a primitive error reporting an unaccounted remainder in both directions, plus the frozen 21-scenario table driven through both composition surfaces: positional 16 compared / 0 divergences, shared cursor 11 compared / 0 divergences, 5 state-operation scenarios not drivable and 5 scenarios whose verdict needs a caller-supplied offset not expressible on the cursor surface), `file_info_identity_test` (7 cases: real fstat kind/size/identity, per-observation size projection, same/different file, unavailable-at-value-level unknown, closed invalid_state, other kind, cursor non-interference), `direct_cursor_position_test` (5 cases: positional vs cursor write, native duplication shares the cursor, independent opens, resize does not move the cursor, zero-length no-op), `direct_composition_test` (7 cases: real-file full read/write, real EOF-before-full prefix, positional placement, cursor advancement, empty request, rejection precedence), `direct_w01_consumer_probe` (3 cases: explicit-close W-01 trace, RAII trace, same-file comparison). The composition tests were checked against mutation: dropping the buffer advance (`subspan(confirmed)` → `subspan(0)`), dropping the offset advance (`offset + confirmed` → `offset`), making the direct stop adopt the reference rule's `eof` reason, dropping the `impossible_count` reason, and rotating the reference projection's `write_no_progress` category each fail `direct_composition_fault_test`, as do the two certainty mutations (the adapter claiming `accounted` for every stop, and claiming `unknown` for every stop). A primitive-retries-the-remainder mutation of `write_at` (looping until the requested length is satisfied) fails both primitive write cases in `direct_composition_fault_test` (checked case by case; the shared runner stops at the first failing case) while the ordinary real-file `blocking_file_write_test` still passes under it, which is why the primitive boundary is pinned at the native-call seam rather than on a real file |
| Protocol/model evidence | None claimed. This slice has no protocol; the request lifecycle model remains Phase B |
| Publication/thread evidence | None claimed. THREAD-01's File row (caller serialization, no outstanding borrow) is unchanged and not exercised here; no concurrency claim is added. A caller that closes or mutates the File while a direct call is in flight violates that row's serialization, so it is outside the contract rather than a defined outcome a call can report. LIFE-01's borrow rule is the reason the direct path adds no reference counting or deferred close: a direct call completes before it returns, so there is no outstanding borrow to track, and enforcement over a borrowed resource belongs to the request path (#394/#395). LIFE-02's `native_handle()` borrow is unchanged |
| Backend/kernel evidence | Direct Linux syscalls only (`pread`/`pwrite`, `read`/`write`, `fstat`, `ftruncate`, `fdatasync`/`fsync`). Close and transfer fault injection use the test-only seam at the native-call boundary, so the counts and reasons are deterministic while the production build contains no seam state. Configuration matrix: debug with liburing disabled (34/34), debug with `--liburing=y` (37/37, real-kernel smoke included), release with liburing disabled (34/34), targeted ASan+UBSan over the six new binaries (all pass). The project-wide `asanubsan` mode does not build in this environment: `src/async/fiber_ctx.cpp` fails on a sanitizer attribute directive under `-Werror`, a pre-existing condition in an untouched TU |
| Build boundary | `direct_w01_consumer_probe` includes only the two direct headers, declares (without including) `sluice::async::{AsyncIoContext, AsyncBackend, Scheduler, Fiber, ApplicationRuntime}` and `detail::RequestArena` and asserts each is incomplete, and links as `g++ … -lsluice_core` with no async or liburing library; `ldd` shows libc/libstdc++/libgcc/libm only. `rg -n 'async|scheduler|fiber|completion|request_arena|request_handle|runtime_task|ApplicationRuntime' include/sluice/file_resource.hpp include/sluice/blocking` returns no match |
| Status | Direct composition helpers: VERIFIED, ERR-02 included — the outcome publishes the failed attempt's effect certainty (`accounted` / `unknown`) from the shared rule, so the direct path represents the unaccounted remainder rather than arguing it away; the request-side representation stays owned by #400. Minimal metadata and same-file identity: VERIFIED on the direct path; the request-side forms remain unassessed and owned by #400. Explicit close and RAII error boundary: VERIFIED. Direct invocation / W-01: VERIFIED. Core-only consumer build boundary (V25): VERIFIED. Installed-header surface: NOT_ASSESSED (no install target exists). A1 rows closed by this slice: exact/all composition, `file_info`/size/identity, close/destructor/move-assignment boundary, zero-length no-OS-call, EINTR retry |

### A2 limitations and recorded boundaries

- **No-progress representation and the composition authority.** The authority
  for a stop is the shared state itself — `CompositionStop`, the accumulated
  `confirmed_bytes` and the primitive's own error — decided once by
  `detail::compose_progress`/`compose_error` and published by each path as its
  own representation. `detail::composition_error` is one such representation: a
  reference `IoError` projection with test consumers and no production consumer,
  not a second authority. The direct outcome carries a structured
  `write_no_progress` stop instead of adopting that projection's `invalid_state`,
  so the A1-recorded SEM-05 ambiguity is not canonized in the public contract.
  The projection keeps its reference-model choice, and a root decision on the
  category would move the projection without moving the canonical stop state.
  Because the two representations differ over two stop reasons, they are driven
  side by side from the same injected primitive sequence by
  `every_stop_reason_is_pinned_against_the_oracle_error_rule`, which pins the
  canonical stop state on both sides and records the split: `complete`,
  `primitive_error` (reason and native detail identical) and `impossible_count`
  agree, while `eof_before_full` and `write_no_progress` differ only in
  representation, the direct side reporting no `IoError` where the reference
  projection names one.
- **Effect certainty (ERR-02; SEM-05's OS-error row).** `CompositionOutcome`
  carries `remaining`, and `detail::composition_effect_certainty` decides it from
  the shared state: a completed, EOF-before-full or no-progress stop accounts for
  its remainder, while a primitive error and an impossible count report
  `unknown`, in both directions. Direction is deliberately not the
  discriminator, for the reason `failed_dispatched_attempt` already records — a
  failing read may have filled part of the caller's borrowed buffer (LIFE-01),
  and a failing write may already have reached the file. The write side has a
  concrete Linux counterexample: `write(2)` documents an error reported for a
  writeback failure, and the NFS write path (`nfs_file_write`, `fs/nfs/file.c`)
  returns a negative result after `generic_perform_write()` accepted bytes —
  either through `generic_write_sync` or through the writeback-error check that
  overwrites a positive result. The earlier claim recorded here, that a `-1`
  return from a direct regular-file primitive proves the attempt transferred
  nothing, is therefore withdrawn as too broad: v1 does not exclude network
  filesystems from the regular-file domain. The accounted side is limited to
  stops whose primitive reported a successful count. A direction-sensitive
  refinement (a failing read accounting for itself) is deliberately not claimed,
  because it would need a per-platform primitive contract with evidence rather
  than another assumption. With the field present the direct outcome is no longer
  a prefix-only terminal in the sense of `detail::prefix_only_terminal_can_carry`;
  the narrower shape that predicate names stays the request-side terminal. The
  field is pinned at every stop, and in both directions, by
  `direct_composition_fault_test`.
- **Cursor-surface range scenarios.** A shared-cursor call supplies no offset, so
  the offset-range step has no operand there: five scenarios whose verdict
  depends on the caller's offset (two range cases, two closed/access cases and
  one accepted-past-EOF case) are reported as not drivable on that surface. The
  length half of the range rule is covered on both surfaces.
- **Durability.** The direct integration sequence (completed write, resize grow
  and resize shrink, each followed by `sync_data`/`sync_all`) is evidence of the
  observable half only. It is not a power-loss durability proof, and the
  request-side half of the V27 row keeps its #400 owner.
- **Durability coverage of a completed resize (A1 row).** Its direct half is now
  CONFORMING; the row keeps its `IMPLEMENTED_UNVERIFIED` status for the
  request-path evidence owned by #400.
- **Open A1 items untouched.** The `bad_alloc` category (#399), the request-side
  effect/cancel representation, and the undisclosed io_uring transfer limit
  remain as recorded; this slice does not change root, error categories or
  request paths.

## B1-1 review record — Issue #394, standalone RequestCore protocol substrate

This record covers the standalone substrate only. The assessment-table row
above stays NOT_ASSESSED for the production surface: no production request
path, backend or public handle changed in this slice, and #394 remains open
for the B1-2+ integration slices. A fresh-context adversarial review returned
`B1_1_FAIL` on the first revision; Corrective-1 fixed the four blockers
(publication-target last access, settled-work control acquisition, health's
absent acceptance effect, unconstrained E capability chain) and re-ran all
evidence; the numbers below are the post-correction state. Detailed evidence,
the happens-before argument, the mutation matrix and the section 13
correspondence tables are in
[b1-1-request-core-evidence.md](b1-1-request-core-evidence.md).

| Field | Content |
|---|---|
| Requirement scope | REQ-01 (identity = context+slot+generation, generation advance and exhaustion retire, copied-ID lookup/cancel token stale after public release even while internal pins live); REQ-02 (one acceptance linearization point serialized with admission close, reservation is not acceptance, rollback leaves no residue, descriptor/borrow facts copied into core-owned bounded storage, accepted-but-undispatched stays discoverable); REQ-03 substrate halves (orthogonal lifecycle facts rather than one enum, at most one immutable terminal, publication gated on retirement of every borrow-touching reference, control records may outlive publication while incapable of touching borrowed state); REQ-04 substrate halves (publication as a release boundary: payload write then ready release-store as the publisher's last target access, consumer acquire pair, no result revision after publication); REQ-05 (reclaim predicate over the substrate's public-binding/E/C/publication facts, handle release before internal retirement pins without blocking the releaser, slot reuse reinitializes under the generation discipline; the observer-delivery conjunct joins at #396 integration); REQ-06 core-local settlement liveness; BOUND-01/02 (bounded slot table, transient capacity vs permanent exhaustion); the identity-validity basis of HANDLE-01; VERIFY-03 L1/L5 core-local halves; VERIFY-04 V04 and V05 substrate halves, V09, V24 (request-id generation facet) and V26. Excludes the public Request/handle API, observers, progress, backends, shutdown and every production migration obligation (B1-2 onward, #395–#401) |
| Implementation | `include/sluice/async/detail/request_core.hpp` + `src/async/detail/request_core.cpp`: a bounded, address-stable, mutex-serialized `RequestCore` (detail namespace) with reservation/acceptance/rollback, admission close and admission health (a health failure closes the same admission gate `admission_open()` reports; both refuse new reservations and re-refuse acceptance at the commit point), cancel arbitration, terminal selection with the admissibility floor, a frozen E capability chain (acceptance installs the first E, the last E retires only once a terminal holds the outcome, no new E after a chosen terminal), C pin acquisition refused on settled work, publication begin/complete with an outside-lock caller-target window, synchronous final-release reclaim, and generation advance to permanent retire. Canonical terminal representation is `sluice::detail::IoOutcome` (success with confirmed bytes, failure with confirmed prefix and effect certainty, `canceled_before_dispatch`). Tests: `tests/request_core_test_driver.hpp` (PublicationTarget with seal-before-ready ordering, the ready release-store contract, and post-ready/post-destroy/misordered publisher-access audits; FakePhysicalDriver deterministic physical-event injection), `request_core_protocol_test.cpp` (34), `request_core_publication_test.cpp` (11), `request_core_consumer_probe.cpp` (2, incomplete-type host-neutrality asserts plus a public-surface lifecycle). Build: three additive targets in `xmake/tests.lua`, each compiling the substrate TU and linking only `sluice_core`. No AsyncIoContext/ThreadPool/io_uring/Scheduler/Fiber/WaiterToken/RoutingLease/ProgressSource wiring exists |
| Change | Additive only. No production file's behavior changed; production admission, `Completion` authority and `RequestHandle` are untouched and remain the old authority until the B1-2+ migration slices. No requirement, error category or public contract was amended |
| Semantic/regression evidence | `request_core_protocol_test` 34/34 and `request_core_publication_test` 11/11 (debug and ASan+UBSan via the clang toolchain; the project-wide `asanubsan` mode cannot use gcc on this host because libasan is absent), `request_core_consumer_probe` 2/2, full suite 37/37 in this configuration, `scripts/check_cpp_comment_authority.py` clean. Load-bearing case names are enumerated in the evidence note (admission/close race and health-gated admission incl. the between-reserve-and-accept refusal, generation aliasing and exhaustion, terminal floor incl. pre-claim cancel and health flag, the E-chain capability rules incl. last-ref premature-release rejection and post-terminal acquisition freeze, settled-work control refusal, pin-gated reclaim with B/C/P-last variants, publication ordering, release racing the publication epilogue with the target destroyed mid-window, the last-publisher-access order/destroy audits, effect-certainty representation for post-accept and pre-claim failures, zero-op) |
| Protocol/model evidence | `formal/tla/RequestCore.tla` (first liveness-carrying model; `FairSpec` with six `WF_vars` conjuncts on named service actions; Corrective-1 adds `NoteHealth`, `AcquireExec`, the RetireExec last-ref guard, and the explicit AcquireCtl settled-work guard) + 20 cfgs gated by `scripts/verify_tla.sh` stage B1-1: clean safety (6,165,549 states generated / 996,436 distinct / 0 on queue, 16 invariants incl. the new `InvNoAcceptAfterHealth`, `InvExecHeldUntilTerminal`, `InvNoExecRevival`), clean liveness (`PROPERTY L1 L5` checked, no violation), 14 invariant-killed mutants, 3 temporal-killed mutants (`MutStrandPostAccept`, `MutLazyReclaim`, and Corrective-1's `MutCtlAfterSettled`, the formal reproduction of the settled-control blocker), and 1 clean separation witness (`MutReadyBeforePayload`, subsumed by the E-chain rule), each kill matched against the exact violated invariant line. Bounds: 2 slots, GenMax 2, MaxCtl 1. Full correspondence tables in the evidence note's section 11 |
| Publication/thread evidence | CPP_MEMORY_MODEL review recorded (evidence note section 5): core state exclusively mutex-guarded; the one lock-free pair is the caller target's ready release-store → consumer acquire, with the payload-visibility obligation stated; every publisher-side target write is sealed strictly before the ready store, making it the publisher's last target access by construction and audited for post-ready, misordered and post-destroy access; the release-racing-epilogue analysis covers both interleavings with the P pin and full-identity epilogue resolution; reclaim has a zero-gap `reclaimable ⇒ reclaimed` postcondition per release call for the `C = 0` case, while the `C > 0` window is closed by the explicit `acquire_control` settled-work guard. Cross-thread instrumentation and TSan are explicitly not claimed (deterministic single-thread tests only); multi-threaded publication evidence belongs to the public-surface/threading slice |
| Backend/kernel evidence | None claimed. The substrate is host-neutral and involves no backend, kernel or filesystem; physical events are injected by the fake driver |
| Status | Production admission and slot lifecycle: NOT_ASSESSED, unchanged (this record adds substrate evidence only; ThreadPool/io_uring request paths remain the current authority until their migration slices). The standalone substrate's protocol obligations — V04/V05 substrate halves, V09, V24 request-id facet, V26, L1/L5 core-local under declared fairness — carry the recorded deterministic-test and TLC evidence above for this branch. Open: production integration (B1-2+), observer-delivery conjunct of REQ-05 (#396), progress-driven reclaim (#397), backend refinement (#400), context identity allocation domain (B1-2a), the pre-claim-outcome model action |

**The open "context identity allocation domain (B1-2a)" item above is closed by
the B1-A record below, whose slice supersedes the old B1-2a execution plan; the
generation/aliasing half of that item stays open as recorded.**

## B1-A review record — Issue #394, context-owned RequestCore and context identity domain

This record covers ownership, identity provenance and the migration seam only.
The assessment-table row stays NOT_ASSESSED: no production request lifecycle
moved, so no production request is RequestCore-owned after this slice.

| Field | Content |
|---|---|
| Requirement scope | REQ-01 (one context identity domain with explicit no-wrap/exhaustion behavior; context identities must not alias while live; a raw address is not identity across destruction/recreation), ARCH-01/ARCH-02 (`IoContext` owns RequestCore; the backend must not own it, and must not allocate public request identity), BOUND-01 (`request_capacity` is a context budget; exhaustion semantics) and HANDLE-03 (a context ID prevents cross-context identity confusion; no registry and no safe-handle promise). Excludes every production cutover obligation (B1-B ThreadPool, B1-C io_uring), the public `Request<T>`/handle API, observers, progress, cancellation, shutdown, and the A1/A2 direct-path rules |
| Implementation | `AsyncIoContext` (the root's `IoContext` before the public rename) gains `std::unique_ptr<detail::RequestCore> core_`, allocated in its constructor and transferred untouched by both move operations, so the core object's address, slot table, generation state and identity survive a legal context move. `include/sluice/async/detail/context_identity.hpp` + `src/async/detail/context_identity.cpp` (listed in `xmake/libraries.lua` because the `src/async/*.cpp` glob misses `detail/`) hold the single identity domain: `claim_context_identity(cursor)` is the domain algorithm and `allocate_context_identity()` is its fail-fast production entry point. `src/async/detail/request_core.cpp` joins the library for the same reason, since the context owns the core in production. Backend seam `AsyncBackend::adopt_context_identity(detail::ContextIdentity) -> std::size_t` is a **pure** private virtual (`friend AsyncIoContext`, mirroring `resolve_identity_state`): every backend states which slot table carries the context identity and its capacity, or explicitly returns 0 for "no slot table". `RequestArena` no longer receives an identity at construction and gains `adopt_context_identity`; `ThreadPoolBackend` and `UringAsyncBackend` (including its no-liburing stub) implement the seam. `arena_context_identity()` and `context_core_for_test()` are `SLUICE_ASYNC_INTERNAL_TESTING` surface. Evidence target: `tests/request_core_ownership_test.cpp` |
| Change | Context construction now allocates one identity from the single domain, installs it into the backend slot table, and constructs the context-owned core with that identity and the reported capacity; a context move or move assignment transfers the same core instead of re-homing state. The two backend-local identity namespaces (`next_backend_id()` with the magic bases `0x54500000` / `0x55720000`) are deleted; disjointness between backends is now structural (one counter) instead of conventional. The identity cursor latches exhaustion before handing out its last value, so no claim can follow a wrapped counter. `RequestHandle`, `resolve_identity_state`, `identity_of`, `supports_request_identity()`, `Completion` authority, the arena slot lifecycle and all publication behavior are unchanged. Production ThreadPool and io_uring requests still route exclusively through `RequestArena`: the core is idle for them (evidence below) |
| Semantic/regression evidence | `request_core_ownership_test` 8/8 (ThreadPool) and `request_core_ownership_uring_test` 9/9 (real ring), full suite 42/42 with `--liburing=y` in debug and release, 38/38 in debug without liburing, targeted ASan+UBSan with leak detection over both ownership binaries (8/8 and 9/9; the project-wide `asanubsan` mode still cannot link gcc on this host because libasan is absent, so the sanitizer run uses the clang toolchain), `scripts/check_cpp_comment_authority.py` clean. Load-bearing cases: `move_transfers_the_same_core` and `move_assignment_transfers_the_same_core` (same core object, identity, capacity, accepted slot and generation across a context move and across a move assignment; the moved-from/move-assigned source holds no second core; the moved-in slot table and core carry the same identity, and a request submitted afterwards resolves through it), `backend_slot_table_carries_the_context_identity` (two backends constructed without a context share one unadopted slot-table identity; each adopted slot table carries its own context's identity; distinct contexts hold distinct identities), `foreign_and_released_identities_do_not_resolve` (own context resolves, foreign context rejects, empty handle rejects, identity survives a context move, a released identity stops resolving), `production_request_is_not_request_core_owned` (backend slot in use and terminal ready while every core fact stays zero: no mirrored reservation, acceptance, terminal, publication or binding; the core admission gate is untouched), `adoption_seam_carries_no_lifecycle_authority` (a recording backend adopts nothing at construction, adopts exactly once at context construction, the core carries the identity the backend received plus the capacity it reported, and neither adoption nor the move changes a canonical core fact), `domain_identities_are_not_reused_across_contexts` (eight live contexts, all nonzero, pairwise distinct), `identity_domain_terminates_at_exhaustion` (a fresh cursor hands out 1 then 2 and never repeats; a cursor seeded at the last value hands that value out once and then reports exhaustion on every further claim instead of wrapping), `uring_context_carries_the_context_identity` (same identity, capacity and no-mirror facts through the real io_uring backend; the case reports a skip, counted separately from passes, when the ring is unavailable) |
| Mutation evidence | Eight production mutations were applied, force-rebuilt (`xmake build -r <target>`) and run against both ownership binaries; each was killed by a named assertion or by the compiler, and the unmutated baseline passed after restore. The force-rebuild is per-target and that target compiles its own async translation units and links only `sluice_core`, so it never links `sluice_async` and the stub-archive hazard above does not apply to these runs. Move (constructor) re-homes a fresh core → `move_transfers_the_same_core`. Move assignment re-homes a fresh core → `move_assignment_transfers_the_same_core`. Slot table ignores the adopted identity → `backend_slot_table_carries_the_context_identity` (and the move-assignment identity pairing). Identity domain returns a constant → `backend_slot_table_carries_the_context_identity`, `domain_identities_are_not_reused_across_contexts`, `move_assignment_transfers_the_same_core`. Identity claim falls back to a plain `fetch_add` → `identity_domain_terminates_at_exhaustion` (the domain re-hands-out 1 after the boundary). Core budget hardcoded instead of the reported capacity → the three capacity assertions. Production acceptance mirrored into the core → `production_request_is_not_request_core_owned` (named failures, then a normal failing exit: the case drains the core it observed so the violation is reported as a FAIL rather than masked by a destruction terminate). A backend omits the seam → rejected at compile time, since the seam is pure virtual |
| Protocol/model evidence | No formal state added, and no TLC configuration re-run. B1-A does not touch `src/async/detail/request_core.cpp` or its header, so the B1-1 model's actions and invariants still correspond to the same C++ transitions. The context identity domain is outside the modeled protocol: `RequestCore.tla` carries `Identity(s) == [slot, gen]` for a single implicit core, and a foreign or unadopted context is rejected by an identity comparison before any modeled action runs — the same class of pre-state rejection as an out-of-range slot index. Modeling context identity would add a state dimension with no request-lifecycle transition to constrain, so it is deliberately not invented here |
| Publication/thread evidence | No new lock and no changed happens-before edge. The identity domain adds one relaxed atomic read-modify-write whose only cross-thread property is unique-value allocation: two contexts constructed on different threads receive distinct identities, and the exhaustion marker is latched with a compare-exchange, so no ordering carries any other state. The identity is written into the backend slot table inside the context constructor, before the context is reachable by any other thread, and it is immutable thereafter: `RequestArena::context()` and `adopt_context_identity` are the only accessors, adoption has exactly one call site (the context constructor, on the calling thread) and no worker, poller or delivery path reads the field, so no legal interleaving pairs an adoption with a read and no mutex is added for one. The context move remains the pre-existing `unique_ptr` transfer under the caller's normal external synchronization. Adoption returns no owner, borrows nothing, and the backend never receives the core, so no delivered interleaving is affected. Cross-thread instrumentation and TSan are not claimed; deterministic single-thread evidence only, as in B1-1 |
| Backend/kernel evidence | ThreadPool evidence is a real backend with a live worker. io_uring evidence uses the real kernel ring (`UringAsyncBackend::available()` true on this host, liburing 2.13); when no ring is available the io_uring case is reported as skipped and counted separately from the passing cases, never as a pass. The io_uring case reads a real file; no filesystem-behavior claim is added and the A1-recorded io_uring gaps stay with #400. Both backend headers and both no-liburing stubs are compiled by the configurations above. Reproduction note: the local `xmake -r` rare rebuild can leave `libsluice_async.a` built from the no-liburing stub objects; that reproduces on the base commit, is unrelated to this slice, and is avoided by an ordinary build or `xmake clean -a` |
| Authority-contraction audit | Removed as a direct consequence of the identity move: both backend-local identity namespaces (`ThreadPoolBackend::next_backend_id()`, `UringAsyncBackend::next_backend_id()` with their magic bases) and the arena's construction-time identity parameter. **No production request authority moved.** `RequestArena` remains the only accepted-set authority for production requests, and every legacy backend lifecycle mechanism — arena slot states, ready ring, dispatch queue, worker lifecycle, cancel intent, waiter/routing leases, publication bindings — stays required, because the same requests still run through it. `ContextIdentity::for_testing` stays: it is test-only construction for the standalone B1-1 suite, not production authority |
| Status | Context-owned RequestCore, the single context identity domain and the migration seam: VERIFIED for the requirement scope named above at the head of this record's PR, under the configurations listed in the evidence rows (deterministic tests, mutation kills, ASan+UBSan, real-kernel io_uring). Production admission and slot lifecycle: NOT_ASSESSED, unchanged — ThreadPool and io_uring requests remain on `RequestArena`, verified mechanically by the idle-core assertion, not merely by inspection. Open and explicitly not claimed here: ThreadPool cutover (**B1-B**), io_uring cutover (**B1-C**), the core handoff half of the seam (no consumer exists yet; B1-B extends this seam when its cutover needs the core), the public `IoContext` rename, and formal coverage of context identity |

### B1-A mechanism justification (AGENTS.md gate)

Every runtime item this slice adds, against the gate's questions: what fact it
represents and which authority owns that fact; what requirement or failure
justifies it; whether it is derivable; its classification; and which mechanism it
replaces, with the condition that permits removal.

| Mechanism | Fact / owning authority | Justification | Derived? | Classification | Replaces; removal condition |
|---|---|---|---|---|---|
| `AsyncIoContext::core_` (`unique_ptr<RequestCore>`) | This context owns that RequestCore (owner: `IoContext`) | ARCH-01/ARCH-02; REQ-01 stable identity across legal moves. Indirect because `RequestCore` is non-movable (address stability plus a mutex), which would otherwise make the context non-movable | No | Authority (ownership) | Replaces "no core in production"; removed only if the core ceases to be context-owned, which no v1 requirement allows |
| `claim_context_identity` + the domain cursor | The single context identity domain and its cursor value (owner: the domain) | REQ-01: one domain, no aliasing while live, exhaustion fails before wrap can make an old identity valid; HANDLE-03 | No | Authority | Replaces both backend-local `next_backend_id()` counters; no removal condition (the domain is the v1 authority) |
| `kContextIdentityExhausted` (0 as the sticky marker) | "This domain has terminated" (owner: the same cursor) | REQ-01's no-wrap clause: without the latch, any state that survives an exhaustion would resume at 1 and alias a live context | No; it is cursor state, not a summary | Authority | None; it is the same counter, not a second mechanism |
| `adopt_context_identity` seam + the three implementations (incl. the no-liburing stub's explicit `return 0`) | Which slot table carries that identity and what its capacity is (owner: the backend slot table; the identity value belongs to the domain) | ARCH-02 (the backend must not allocate public identity) and BOUND-01 (the core must be sized by the table it will serve); pure virtual so no backend can silently keep an unadopted table | No; the slot table is backend-owned state the context cannot compute | Migration seam (temporary) | Replaces the arena's construction-time identity argument; removable once B1-B/B1-C move the request lifecycle into the core and the seam has no consumer |
| `RequestArena::adopt_context_identity` + `ContextIdentity context_{}` | The identity the arena validates its requests against (owner: the context, installed into the arena) | REQ-01/HANDLE-03 cross-context rejection; the arena must reject a foreign context's handle | No; but no new state — the same field, installed instead of constructed | Compatibility state | Replaces the constructor parameter; removed with the arena itself under B1-B/B1-C |
| `context_identity_exhausted_fail_fast()` | The domain's exhaustion diagnostic (owner: the domain cursor) | REQ-01 requires explicit exhaustion behavior rather than silent reuse | Yes: it reports the cursor's marker and adds no independent fact | Derived diagnostic | None; same family shape as `request_arena_generation_exhausted_fail_fast` |
| `RequestCore::context_` / `capacity_` (B1-1 state, reused) | Summaries of the once-installed identity and the reported capacity (owner: the domain and the slot table) | Lets the core reject foreign identities and bound its own table | Yes | Derived summary (non-authoritative; kept, not a second source of truth) | None in this slice |
| `context_core_for_test()` / `arena_context_identity()` (macro-guarded) | Observation of this slice's ownership and identity facts (owner: the production objects) | This slice's evidence need: stable ownership and provenance must be asserted mechanically, not by inspection | Yes | Test-only evidence | None; withdrawn with the B1-A/B1-1 test targets |
| `CoreCaseCleanup` in the evidence target | Drains the core a failed case observed (owner: the test harness) | Keeps a named assertion failure the reported evidence: without it, destroying a non-quiescent core terminates and masks the violated assertion | No; test-harness tooling outside the production binary | Test-only evidence | None; test tooling |




## B1-B review record — Issue #394, ThreadPool atomic RequestCore authority cutover

This record covers the first production authority move. After this slice every
ThreadPool request's acceptance, identity, accepted-set membership, execution
responsibility, terminal arbitration, canonical result, publication,
public-binding release and reclaim belong to the context-owned `RequestCore`;
ThreadPool owns only physical dispatch/execution facts. io_uring is untouched
and remains on `RequestArena` until B1-C.

| Field | Content |
|---|---|
| Requirement scope | REQ-01 (identity = context+slot+generation allocated by the core; public lookup/cancel token stale immediately after public binding release even while a publication pin holds the slot), REQ-02 (single acceptance linearization under the core's serialized authority; validation before reservation; reservation is not acceptance; post-accept dispatch failure converges through the terminal path; accepted-but-undispatched work keeps an owner), REQ-03/REQ-04/REQ-05 production halves for ThreadPool (publication gated on borrow-touching retirement; the `Completion<T>` ready release/acquire pair as the compatibility visibility edge; reclaim under the core's canonical predicate with synchronous final-pin reclaim), SEM-03 steps 3–7 precedence and the zero-op immediate-publication rule on the request path, INV-02 (bounded ThreadPool submit/cancel initiation), ARCH-02/BACKEND-01 (backend owns physical execution only), BOUND-01/02 (capacity and queue bounds), HANDLE-01/03 basis (public binding semantics without the final `Request<T>` API), VERIFY-03 L1/L5 ThreadPool-mapped environment assumptions, VERIFY-04 V04/V05/V09/V24/V26 production ThreadPool halves. Excludes io_uring cutover (B1-C), the public `Request<T>` API (#395), Observer (#396), ProgressSource/no-lost-wake (#397), the full cancel/effect matrix (#400), shutdown state machine (#401) and any queue/lock-free/performance redesign |
| Implementation | `ThreadPoolBackend` drops its `RequestArena` entirely and adopts the context-owned core through the new `AsyncBackend::adopt_request_core(detail::RequestCore*)` seam (default false; the context constructor calls it after B1-A's `adopt_context_identity`); the submit transaction becomes validate → `core_->reserve()` → scratch/delivery-record write → tentative Completion binding → `core_->accept()` (the linearization point; installs the public binding and the first execution ref) → either inline zero-op publication or physical dispatch. The zero-op path acquires a core control ref before publication begins — the deferred ready-event obligation is pinned for its whole life — and `poll()` discharges the owed event and releases that ref, whose final retirement reclaims the slot synchronously (the review-corrective delivery pin; the first production consumer of the B1-1 `acquire_control`/`release_control` pair). The worker pops the dispatch ring and takes over execution under `work_mtx_` via `core_->claim_execution()` (late/stale claims skip cancelled work), runs the blocking syscall, offers the physical outcome via `core_->offer_terminal`, retires the borrow-touching ref via `core_->release_execution`, and queues the publication drive. `poll()`/`wait_one()` drive publication: `begin_publication` (granted only after terminal + E retirement) → compatibility write to the caller's `Completion` outside all locks → `complete_publication` → ready-sink event. `Completion` gains a core binding (`install_core_binding_for_backend`/`core_binding_for_backend`; reset/destructor call `release_public_binding`); the arena binding remains for io_uring. Cancel resolves the request key from the binding, removes queued work and arbitrates in the core: pre-claim cancels win a zero-effect terminal, retire the execution ref and queue publication; post-claim cancels record intent only. `AsyncIoContext::~AsyncIoContext` now retires the backend before the core (workers call into the core); `outstanding()` is core-derived. Evidence target: `tests/threadpool_core_cutover_test.cpp` with reworked deterministic pause gates (submit entry, pre-accept commit, accepted-pre-dispatch, worker dequeue/claimed/outcome-pre-terminal, publication epilogue) and fault injections (dispatch failure, three submit-stage failures) |
| Change | Behavior changes: (1) SEM-03 precedence for ThreadPool — validation precedes reservation, so invalid range outranks capacity and closed admission ranks between them (two A1 GAP rows close for ThreadPool; io_uring unchanged); (2) zero-length data requests are accepted, published at acceptance (`ready()` true on submit return) and never dispatched (A1 zero-op GAP row closes for ThreadPool; the recorded ThreadPool divergence set in `semantic_validation_precedence_test` becomes empty), and their ready event stays owed — under a core control ref — until the next poll discharges it, so a context destroyed with the event undelivered is non-quiescent destruction instead of a silent drop; (3) `request_state()` no longer reports `backend_ready` for ThreadPool — a request is publicly outstanding until published, then completion-ready (an internal fact stops being a public state; the root's HANDLE-01 table has no such state); (4) cancel racing a zero-op now reports already-terminal instead of racing; (5) the backend's `outstanding()` and destruction quiescence checks read core facts instead of arena counters. No public API was added, removed or retyped; `Completion`, `RequestHandle`, waiter registration and the ready-sink events keep their shapes |
| Semantic/regression evidence | `threadpool_core_cutover_test` 14/14 (asanubsan, debug, release, TSan): pre-accept rejection leaves no core residue; range precedes capacity under saturation; the accepted-undispatched window holds a live core owner (E=1, binding, no terminal) with an empty dispatch queue; cancel in that window wins pre-execution and converges through the terminal with zero syscalls, and the stale dispatch entry the losing submitter still enqueues is drained by its worker before teardown; injected post-accept dispatch failure returns an accepted request whose terminal is `backend_error` (V05) while no worker ran; worker claim/running cancel keeps `cancel_intent` with E=1 and the physical outcome still wins (cancel never retires a running borrow); the last physical access (syscall count 1, terminal not yet chosen, E=1) strictly precedes ref retirement and no publication is queued while E lives; the epilogue race (release between result write and `complete_publication`) invalidates the public identity immediately, pins the slot on the publication inflight flag, and reclaims synchronously on the final pin with no unrelated new I/O (V26), after which the slot is reusable; zero-op publishes at acceptance with zero syscalls and discharges its owed delivery event on the next poll; the corrective's capacity=1 case pins the whole reset-before-poll sequence — after the Completion is reset before any poll the slot stays accepted (published, binding released, C=1) and the owed event survives, a new submit is refused `would_block` with the owed record untouched, the next poll delivers exactly one ready event carrying the old request key, the final pin retirement reclaims synchronously with zero new syscalls, and the slot then serves a new request at generation+1; admission close between reserve and accept rejects with `invalid_state` and rolls the reservation back (V04 production shape); waiter registration/delivery rides the publication with the registered token and lease; a stale identity does not resolve after slot reuse (V09); four concurrent submissions across three workers all publish, reclaim and run every syscall on workers (INV-02). `request_core_ownership_test` 8/8 with the flipped premise (`production_request_is_request_core_owned`: live core facts during flight, core publication/binding ownership, reclaim on release; the arena idle-core case is gone with the arena). Full suite: 39/39 asanubsan without liburing, 39/39 debug, 39/39 release, 43/43 debug `--liburing=y`, 43/43 release `--liburing=y` (real-kernel io_uring smoke intact proves B1-C remains unmigrated); `semantic_validation_precedence_test` now reports 0 recorded ThreadPool divergences; the direct zero-op precedence cases (`file_access_precedence_test`) drive the progress service at least once after a zero-length submit so the deferred event is discharged before teardown; `scripts/check_cpp_comment_authority.py` clean |
| Protocol/model evidence | `formal/tla/RequestCore.tla` is unchanged: the B1-B production mapping introduces no semantic transition outside the accepted B1-1 protocol. Production correspondence: submit validate+reserve ≙ Reserve; rollback ≙ Rollback; `core_->accept` ≙ Accept (the zero-op path is the model chain Accept∘PhysicalOutcome(ok)∘RetireExec compressed into one call, matching the B1-1 substrate); the zero-op's deferred compatibility delivery ≙ `acquire_control` at obligation creation and `release_control` after the sink event, whose final retirement triggers the modeled synchronous Reclaim even when the public binding was released first; `close_admission` ≙ CloseAdmission; worker pop+claim ≙ ClaimExec (a cancelled-in-window queue entry leaves the claim disabled — modeled as cancel-then-no-claim); syscall→`offer_terminal` ≙ PhysicalOutcome; pre-claim cancel win ≙ CancelRequest cancelwin; post-claim cancel ≙ CancelRequest intent; final `release_execution` ≙ RetireExec; `begin/complete_publication` ≙ BeginPublish/CompletePublish; `Completion` reset/destructor ≙ ReleaseBind (including the pub=inflight epilogue interleaving); synchronous `try_reclaim` ≙ Reclaim. Stage B1-1 rerun after the cutover and again after the delivery-pin corrective (same jar v1.7.4, same 20 cfgs, exact `verify_tla.sh` command list): safety clean, `PROPERTY L1 L5` clean, 14 safety mutants killed on their named invariants, 3 liveness mutants killed, the MutReadyBeforePayload subsumption witness clean. ThreadPool environment assumptions under which L1/L5 cover the production path: accepted dispatchable work is eventually serviced (workers wait on `work_cv_` and every dispatch push notifies — OS scheduling of a runnable worker is outside Sluice authority); worker physical execution eventually returns (a stalled blocking syscall invalidates the premise, as the root's REQ-06 caveat records); enabled internal publication/reclaim work is serviced (the driver's poll is the service; the ready-wait wake chain carries it, and the fiber runtime's park loop polls through `wait_one`, so a zero-op's pinned event is discharged before quiescence). L2 (the owner observes the wake) remains #397's obligation and is assumed here, not proved |
| Mutation evidence | The corrective's delivery-pin mutations were applied to `src/async/threadpool_backend.cpp`, force-rebuilt (`xmake -r threadpool_core_cutover_test`) and run against the cutover binary; the unmutated baseline passed after restore. Removing only the zero-op `acquire_control` dies on the protocol diagnostic in `poll()` (`release_control` underflow → `threadpool_core_handoff_fail_fast`). Removing the pin acquisition and the `release_control` call together dies on nine named assertions of `zero_op_delivery_pins_the_slot_until_the_event_is_delivered` (pin observation, `would_block` reuse refusal, owed-record survival, exactly-once delivery, old-key identity, synchronous reclaim, reuse). Removing only the `release_control` call dies on `the zero-op slot reclaims after release` plus the non-quiescent destruction terminate |
| Publication/thread evidence | The ThreadPool C++ happens-before chain: the worker's syscall and outcome construction happen-before its `offer_terminal` (same thread, sequenced; the outcome write lands under the core mutex); `release_execution`, the FIFO push (under `work_mtx_`) and the ready-wait signal are sequenced after it. The driver that observes the signal (condition-variable acquire) or polls unprompted pops the FIFO under `work_mtx_` (acquiring the push) and calls `begin_publication`, whose core-mutex acquire establishes visibility of the outcome, returning a by-value payload copy. The delivery thunk then writes the caller's `Completion` storage and executes `state_.store(ready, release)` — the pre-existing compatibility visibility edge, with the storage write strictly sequenced before it. A consumer's acquire load of `ready` therefore synchronizes with the entire worker→core→publisher chain before it reads the result or reuses the buffer (REQ-04; S4/S5). The borrow boundary: the thunk's ready store is the publisher's last access to that `Completion`; `deliver_event` afterwards touches only delivery-record bookkeeping (waiter token/lease), never the completion or caller memory. The epilogue race is closed by the core mutex alone: a release between the thunk and `complete_publication` finds `publication_inflight` (visible ⇒ released), drops `binding_live` and skips reclaim; the publisher's `complete_publication` then publishes and reclaims synchronously. The delivery pin adds no new lock or atomic: both of its transitions run under the core mutex already held by `publish_zero_op_inline`/`poll`, and the zero-op publication completes on the submit thread before submit returns, so no cross-thread delivery interleaving exists in the pinned window. `active_workers_--` is never used as borrow proof — the proof point is offer+release under the core's authority after the last physical access. TSan: the cutover suite and the ownership suite pass under `-m tsan` (real submitter/worker/publisher/consumer interleavings); the Fiber-runtime tests do not compile under gcc TSan because of the pre-existing `disable_sanitizer_instrumentation` attribute directive in `src/async/fiber_ctx.cpp` (same class as the A2-recorded asanubsan blocker), so no TSan claim is made for them |
| Backend/kernel evidence | ThreadPool evidence runs against real regular files on tmpfs/ext4 (pread/pwrite/fdatasync/fsync, real EINVAL on `/dev/null` preserved through `IoOutcome::failure` with native detail). Real-kernel io_uring (`--liburing=y`, liburing 2.13) passes its full smoke and ownership suites on the same binaries, demonstrating the unmigrated path is intact. Locking: context `access_mtx_` → backend `work_mtx_` → core mutex, acquired only in that order (the core never calls out; delivery runs outside `work_mtx_` and outside the core mutex; host hooks are never run under either); delivery records are guarded by `access_mtx_` and never touched by workers |
| Authority-contraction audit | Removed with the cutover: ThreadPool's entire `RequestArena` ownership (slot state machine, generation table, reserve/prepare/commit transaction, `enqueue_in_flight_pin_`, ready ring, terminal arbitration and storage, publication binding, `release_completed_binding`→free, cancel arbitration, waiter storage, admission-closed flag, `accepted_outstanding_`/`slot_in_use_`/`high_water_mark_`/`capacity_rejections_` counters), the `submit_transaction` usage, the `admission_mtx_` admission serialization (the core mutex is now the linearization authority) and the `TerminalResult`-based syscall result path. Retained compatibility mechanisms: the `Completion` dual binding (core flavor for ThreadPool, arena flavor for io_uring until B1-C), waiter/routing records and `ReadyEvent` delivery (#396 owns the successor), `RequestHandleState` mapping. Retained physical mechanisms that look duplicated but are independently required: `prepared_ops_` (physical syscall parameters the core does not store), the dispatch ring (bounded dispatch storage), `active_workers_` (physical worker-busy count for teardown quiescence — a physical fact, not a borrow fact), the publication-pending ring (the progress driver's work list — derived from core eligibility facts, bounded by capacity because every entry pins an unpublished accepted slot; `begin_publication` remains the authority). Corrective delivery pin: `DeliveryRecord.event_owed`/`owed_key` had outlived publication without entering the core's reclaim accounting; the fix materializes no new runtime fact — the obligation is carried by the existing B1-1 `control_refs` (classification: physical/compatibility obligation of the deferred compatibility delivery; replaces `None`; removable only with the compatibility ready-event delivery itself under #396). Temporary: `adopt_request_core` (removed when B1-C migrates io_uring and the seam collapses into the backend interface), the Completion arena flavor (removed with B1-C/#395/#402 consumer retirement) |
| Status | ThreadPool production admission/lifecycle: the B1-B scope above is VERIFIED for this record's PR head under the recorded configurations (deterministic interleaving tests, TSan on the cutover suites, full-suite matrix, real-kernel io_uring regression, stage-B1-1 TLC rerun with mutations). The assessment row stays NOT_ASSESSED as a whole: io_uring remains on `RequestArena` until B1-C, the public `Request<T>` API is #395, observer/progress/shutdown/effect obligations are #396/#397/#400/#401, and the `RequestHandleState::backend_ready` removal is a compatibility-surface change consumed by the B1-C ledger update. **[B1-D update, later commit]** B1-C migrated io_uring without removing the enum value: `backend_ready` now has zero producers and stays with #395/#402 compatibility retirement, and the `adopt_request_core` seam this record marks temporary collapsed into the backend interface contract at B1-D |

## B1-C review record — Issue #394, io_uring atomic RequestCore authority cutover

This record covers the io_uring production request path only. With it, both
production backends run under the context-owned `RequestCore`; there is no
mergeable half-migrated state: `UringAsyncBackend` no longer holds a
`RequestArena` at all, so no second lifecycle authority exists beside the core
on any production path. #394 remains open only for the closure slices it
already names (#395–#401). **[B1-D update, later commit]** #394's migration
scope itself closed at B1-D (cross-backend evidence convergence plus seam
contraction; see the B1-D review record); the #395–#401 obligations are
untouched and keep their owners.

| Field | Content |
|---|---|
| Requirement scope | REQ-01 (io_uring request identity = core-allocated context+slot+generation; public lookup/cancel stale immediately after public binding release even while a transport pin holds the slot), REQ-02 (single acceptance linearization under the core's serialized authority: validate → reserve → transport preparation that can roll back → `core_->accept` → post-accept transport work; pre-accept failures leave no reservation, no borrow, no public identity, no cookie/router/ledger entry; post-accept failures converge through the core terminal path), REQ-03/REQ-04/REQ-05 production halves for io_uring (publication gated on borrow-touching retirement at the original-operation CQE; the `Completion` ready release/acquire pair as the compatibility visibility edge; reclaim under the core's canonical predicate with synchronous final-pin retirement), SEM-03 steps 3–7 precedence and the zero-op immediate-publication rule on the io_uring request path (the previously recorded io_uring halves of the precedence and zero-op GAP rows are closed), INV-02 (bounded io_uring submit/cancel initiation; the submission-mode blocking risk remains a #400 profile item), ARCH-02/BACKEND-01 (backend owns SQE/CQE transport only), CANCEL-01 floors (pre-dispatch cancel wins zero-effect; a control CQE is neither the original result nor proof of original retirement; a racing cancel never erases a confirmed count), BOUND-01/02 (cookies, ledger and control records bounded by capacity/ring depth; no stranded accepted work), VERIFY-03 L1/L5 io_uring-mapped environment assumptions, VERIFY-04 V04/V05/V09/V13/V14/V26 io_uring halves and the V24 request-id facet. Excludes the public `Request<T>` API (#395), Observer (#396), ProgressSource/no-lost-wake (#397), RequestScope (#398), host redesign (#399), the full cancel/effect matrix (#400), the shutdown/poison state machine (#401), and any performance/queue redesign |
| Implementation | `UringAsyncBackend` drops its `RequestArena` and adopts the context-owned core through `adopt_request_core`. The submit transaction becomes validate (SEM-03 1–4, zero-op range skip with offset normalization) → `core_->reserve()` (admission/health then capacity) → prepared-SQE-descriptor and delivery-record write → tentative `Completion` binding → `core_->accept()` (the linearization point; installs the public binding and the first execution ref; a zero-op is terminal at acceptance) → either inline zero-op publication (a core control ref pins the owed ready event; the next `poll()` discharges it and the final pin reclaims synchronously) or dispatch: the dispatch ring holds accepted-undispatched handles; `dispatch_one_locked` acquires an SQE slot, claims execution in the core (a lost claim returns the unused SQE slot and drops the entry), then allocates the monotonic cookie, prepares the SQE, creates the router entry and appends the transport-ledger entry. CQE reaping maps each class to its true role: an original-operation CQE offers the physical outcome to the core (first admissible winner), retires the borrow-touching ref (`borrow_touch_fully_retired`), queues publication, and retires the router entry unless a control CQE is still owed; a control CQE (tagged cookie) releases only the core control ref acquired when the cancel SQE was prepared, never touching the borrow or the outcome. The router entry keeps `control_state` and `terminal_delivered` so it survives until both the original and the control CQE have retired. Poison maps onto the core: `note_health_failure` closes admission, kernel-invisible work (dispatch entries and prepared ledger entries) converges to error terminals through the core, and already-submitted operations keep their real completion path so no fabricated terminal publishes a borrow that the kernel may still touch. Cancel resolves the request key from the `Completion` core binding, removes queued work, and arbitrates in the core: pre-claim wins retire E and queue publication; post-claim cancels record intent and issue at most one cancel SQE per request (coalescing derived from the live router entry's `control_state`). `outstanding()` is core-derived; identity resolution and waiter registration gate on core lookup; the context destructor retires the backend before the core. Evidence target: `tests/uring_core_cutover_test.cpp` (20 cases) with deterministic pause gates (submit entry, pre-accept commit, accepted-pre-dispatch, publication epilogue), submit-stage and dispatch-failure injections, a no-kernel fake submit that advances the SQ cursors so CQEs arrive only through `inject_cqe_for_test` (fully deterministic ordering), and real-kernel cases on the live ring |
| Change | `UringAsyncBackend` loses the `RequestArena`, the arena submit transaction, the per-slot cancel scratch vector, the `RouterEntry::deferred_terminal` pair (value + mirror bool) and the backend admission-closed mirror; `Completion`'s arena binding flavor and `install_binding`/`submit_transaction` compatibility surface remain but now have zero production consumers (removal condition below). SEM-03 precedence and zero-op behavior change on the io_uring request path as the ledger rows record; no requirement, error category or public contract was amended |
| Semantic/regression evidence | `uring_core_cutover_test` 20/20 (debug, release, asanubsan, TSan, real kernel liburing 2.14/kernel 6.18): pre-accept rejection leaves no core residue; range precedes closure and capacity; the accepted-pre-submit window holds a live core owner (E=1, binding, no claim, no SQE/cookie/ledger) and cancel in that window wins pre-execution with zero transport facts while the losing submitter's drain drops its own queue entry; original-CQE-first defers nothing semantically — the physical outcome is chosen and E retired at the original CQE while the control obligation still pins the slot, publication proceeds, the released binding stops resolving, and the control CQE reclaims synchronously with no new I/O; control-CQE-first retires only the control obligation (E stays held, no publication) and the late original CQE canonicalizes the real outcome on the same generation; a racing cancel never erases the confirmed short count; a stale cookie's CQE cannot affect the reused slot and cookies never reuse across generations; the stale public identity stops resolving after slot reuse; the zero-op publishes at acceptance with zero transport facts and its owed event survives a reset-before-poll at capacity 1 (refuses new admission `would_block`, delivers exactly one event, reclaims synchronously, then serves the next generation); submit-stage failures at reserve/prepare/commit all roll back with no transport leak; injected dispatch failure and sequenced submit failure (`-EIO` poison) converge accepted requests through the terminal path — the never-visible one to an error terminal, the kernel-visible one to its real physical outcome — with the poisoned backend refusing new submissions with its health failure and nothing ownerless; a running cancel across the poison keeps the router entry routable so the real original CQE still converges request A to its success terminal while the cancel obligation's ref retires in the recovery walk; the publication-epilogue release race pins the slot on the publication-inflight flag and reclaims synchronously; waiter registration rides publication with the registered token and lease; four concurrent submissions all publish and reclaim with no cookie leak; real-kernel read converges through the core publication and reclaim; the real-kernel cancel target converges to one terminal whose disposition is platform-specific (recorded: this kernel completes the target with native `-EALREADY` → `backend_error` with native detail) and reclaims. `semantic_validation_precedence_test` now drives the io_uring request path against the oracle with 0 recorded divergences (19 compared, 2 not drivable, same as ThreadPool); `semantic_backend_conformance_test` reports 0 recorded uring divergences (zero-op divergences closed); `uring_backend_smoke_test` 8/8 on the real ring; `request_core_ownership_uring_test` 9/9 with the flipped premise (the io_uring request is core-owned; the B1-A idle-core assertion is gone). Full matrix: debug 44/44 and release 44/44 with `--liburing=y`; asanubsan 10/10 on the self-contained seam targets (the project-wide asanubsan sluice_async build remains blocked by the pre-existing gcc/fiber_ctx attribute error recorded in B1-B, unrelated to this slice); TSan passes `uring_core_cutover_test`, `threadpool_core_cutover_test` and `request_core_ownership_uring_test` at the final head; `--liburing=n` builds and passes 39/39 with the honest stub. `scripts/check_cpp_comment_authority.py` clean |
| Protocol/model evidence | `formal/tla/RequestCore.tla` is unchanged: the io_uring production mapping introduces no semantic transition outside the accepted B1-1 protocol — the transport state space (cookies, SQE stages, ledger, router) lives in the C++/backend correspondence and never changes a core action's precondition or effect. Production correspondence: submit validate+reserve ≙ Reserve; rollback ≙ Rollback; `core_->accept` ≙ Accept (the zero-op path is the model chain Accept∘PhysicalOutcome(ok)∘RetireExec compressed into one call, as in B1-B); dispatch pop+claim ≙ ClaimExec (a cancelled-in-window entry leaves the claim disabled; the lost-claim path also returns the unused SQE slot); original-CQE `offer_terminal` ≙ PhysicalOutcome; pre-claim cancel win ≙ CancelRequest cancelwin; post-claim cancel ≙ CancelRequest intent; the control SQE's `acquire_control` ≙ AcquireCtl and the control CQE's `release_control` ≙ ReleaseCtl (the modeled obligation whose retirement pins the slot against reclaim); post-original-CQE `release_execution` ≙ RetireExec; `begin/complete_publication` ≙ BeginPublish/CompletePublish; `Completion` reset/destructor ≙ ReleaseBind (including the pub-inflight epilogue interleaving); synchronous reclaim on the final pin ≙ Reclaim; poison `note_health_failure` ≙ NoteHealth; `close_admission` ≙ CloseAdmission. Stage B1-1 rerun on this branch (same jar v1.7.4, same 20 cfgs, the exact `verify_tla.sh` stage-B1-1 command list): safety clean (6,165,549 states generated / 996,436 distinct / 0 on queue, `Model checking completed. No error has been found`), 14 invariant mutants killed on their named invariants, 3 temporal mutants killed (`Temporal properties were violated`), the MutReadyBeforePayload subsumption witness clean; and the `PROPERTY L1 L5` liveness configuration completed clean on this branch (6,165,549 states generated / 996,436 distinct / 0 on queue, `Model checking completed. No error has been found`). io_uring environment assumptions under which L1/L5 cover the production path: submitted kernel work eventually produces the required retirement evidence (a CQE for every submitted SQE — a stalled filesystem operation invalidates the premise exactly as REQ-06 records); progress service eventually reaps available CQEs (the driver's poll/wait protocol); enabled internal publication/reclaim work is serviced (the same poll drive discharges publications and owed zero-op events). L2 remains #397's obligation and is assumed, not proved |
| Mutation evidence | Eight named C++ mutants (`SLUICE_B1C_MUTANT_*` compile definitions, one xmake target each, run manually and recorded; the clean suite passes after restore). `COOKIE_REUSE` (cookie allocator returns a fixed value) dies on `stale_cookie_cannot_reach_a_reused_slot` — monotonic-consumption, never-reused, and stale-CQE-ineffective assertions all fail, and the stale CQE then corrupts the reused request. `CANCEL_CQE_AS_ORIGINAL_TERMINAL` (the control CQE fabricates a canceled outcome, retires E and retires the entry) dies on `cancel_cqe_first_never_becomes_the_terminal` — the control obligation is observed to retire the borrow, the late original outcome is lost, and the published bytes are wrong. `PUBLISH_BEFORE_BORROW_RETIREMENT` (publication queued before `release_execution`) dies on `uring_core_handoff_fail_fast` — `begin_publication` refuses while an execution ref is live (the core's InvNoPubWhileExec correspondence). `DROP_ORIGINAL_OUTCOME` (negative byte-op CQEs are dropped) dies on the Completion L9 assertion after bounded polling — the stranded request never publishes. `REMOVE_CONTROL_PIN` (no `acquire_control` when the cancel SQE is prepared) dies on `original_cqe_first_defers_until_control_retires` — the slot reclaims before the control CQE and the late release lands on a stale slot (`uring_core_handoff_fail_fast`). `RECLAIM_BEFORE_CONTROL_RETIREMENT` (the router entry retires while the original outcome is still owed) dies on `cancel_cqe_first_never_becomes_the_terminal` — the late original CQE misses the router and the outcome is lost. `STRAND_POST_ACCEPT_SUBMIT_FAILURE` (poison recovery skips the Class-A ledger walk) dies on `post_accept_submit_failure_poison_converges` — the never-visible request never converges. `PREMATURE_ZERO_OP_DISPATCH` (zero-ops forced through transport) dies on `zero_op_publishes_at_acceptance_without_dispatch`. Structural exclusion: "leave Arena terminal/reclaim authority active beside RequestCore" is not expressible post-cutover — the backend no longer contains an arena to mutate; the idle-second-authority proof is the absence of the member itself plus the whole suite passing against the core alone |
| Publication/thread evidence | The io_uring C++ happens-before chain: the kernel publishes completion results into the CQ ring under io_uring's own kernel→userspace ordering (liburing's documented barriers for `io_uring_peek_batch_cqe`); the reaper consumes them on the progress/submitting thread under the context `access_mtx_` and the backend `dispatch_mtx_` (acquired inside `handle_one_cqe`), constructs the `IoOutcome` and offers it through the core mutex (whose acquire makes the physical outcome visible to every later core observation); `release_execution` and the publication-pending push are sequenced after the offer in the same critical section. The publisher pops under `dispatch_mtx_`, calls `begin_publication` (core mutex; by-value payload copy), writes the caller's `Completion` storage and executes `state_.store(ready, release)` — the pre-existing compatibility visibility edge with the storage write strictly sequenced before it — then `complete_publication` and the ready-sink event. A consumer's acquire load of `ready` therefore synchronizes with kernel→backend→core→publisher before reading the result or reusing the buffer (REQ-04; S4/S5). The borrow boundary: the borrow-touching ref retires only after the original CQE (the kernel can no longer touch the caller buffer for that operation; a control SQE never touches caller memory), and the ready store is the publisher's last access to the `Completion` — `deliver_event` afterwards touches only delivery-record bookkeeping. The epilogue race is closed by the core mutex alone (release between thunk and `complete_publication` observes `publication_inflight`, drops `binding_live`, defers reclaim to the publisher's completion — measured deterministically). Lock order: context `access_mtx_` → backend `dispatch_mtx_` → core mutex, acquired only in that order; the core never calls out while held; publication and delivery run outside `dispatch_mtx_`; the wait-source park protocol is unchanged. TSan passes the cutover, threadpool-cutover and ownership suites; the fiber-runtime tests remain excluded from sanitizer claims by the pre-existing gcc `fiber_ctx.cpp` attribute blocker |
| Backend/kernel evidence | Real kernel: liburing 2.14, kernel 6.18.33 (WSL2), tmpfs/ext4 regular files; `uring_backend_smoke_test` (read/write/sync_data/sync_all/zero-op/invalid-offset), `semantic_backend_conformance_test` (0 divergences) and the cutover suite's real-kernel cases (read through core publication/reclaim; cancel convergence with the recorded `-EALREADY` disposition; six rounds of slot reuse with identity resolution) all pass on the live ring. Deterministic seams (fake submit + injected CQEs) cover orderings the kernel cannot schedule on demand (original-first vs control-first, stale cookies) — they do not replace the real-kernel runs. Bounded accounting: cookies ≤ request capacity, ledger entries ≤ SQ ring depth, control records ≤ one per live request (coalesced by the router's `control_state`). Known #400/#401 boundary recorded, not closed: a hard submit failure is treated as kernel-invisible per liburing's atomic-failure behavior, and the recovery fabricates error terminals for those prepared entries exactly as the pre-cutover Class-A recovery did — the root's warning that a negative submit return does not prove kernel invisibility remains an open #401 refinement obligation, now at least preserved verbatim rather than widened; platform-specific cancel dispositions are recorded, not normalized |
| Authority-contraction audit | Removed lifecycle authority: `UringAsyncBackend`'s entire `RequestArena` ownership (slot state machine with `mark_running`/`enqueue`/`record_terminal`, generation table, reserve/prepare/commit transaction, `enqueue_in_flight_pin_`, ready ring, terminal arbitration and `TerminalResult` storage, publication binding and `release_completed_binding`→free path, cancel arbitration with `cancel_intent_`, waiter storage, `resolve_completion` scan, admission-closed flag, accepted-outstanding/slot-in-use/backend-ready counters) — the core is now the only accepted-set, identity, terminal, publication and reclaim authority for io_uring. Removed mirrors (each with demonstrated derivation): the per-slot `CancelScratch::cancel_queued` vector (the coalescing fact is exactly the live router entry's `control_state != none`, checked on the entry the scan already holds), `RouterEntry::deferred_terminal_stored` (a pure mirror of the stored value's own predicate — and with the terminal now offered at the original CQE, the deferred value itself is gone: kernel semantics make the original operation's CQE the single final outcome, and the control CQE carries no information about it, so the entry's lifetime obligation is carried explicitly by `control_state` + `terminal_delivered`), the backend `admission_closed_` mirror (the core's admission gate is the authority; poison closes it through `note_health_failure`), and the dead historical test machinery (router scan/fix-mode diagnostics, `RouterCookieTableForTest`, `SlotHandle`-based arena observation seams, `before_queue_exit` hook — all with zero remaining consumers). Retained physical mechanisms, each with its obligation: the monotonic non-reused cookie allocator (kernel anti-ABA identity, distinct from core generation safety), `PreparedUringOp` per slot (physical SQE parameters the core does not store), the bounded dispatch ring (accepted-undispatched transport backlog), the backend publication worklist `publication_pending_` (retained bounded progress worklist for the migrated io_uring path: the `RequestArena` ready ring → removed, `publication_pending_` → retained as the progress driver's publication queue — a mechanism replacement, not a transfer of lifecycle authority; terminal arbitration, canonical result, publication eligibility, public request identity, public binding lifetime and slot reclaim remain `RequestCore` authority, and `begin_publication` remains the grant; derived from core eligibility facts, non-authoritative, bounded by request capacity; removable only when the compatibility publication/delivery path no longer requires a backend worklist under #396/#397 or their authorized replacement), the `TransportLedger` (prepared-vs-submitted kernel obligations with FIFO physical positions; the poison recovery's walk order), the router entry (cookie→key routing that must outlive both CQE classes when a cancel is in flight), `live_cookies_`/`live_control_sqes_`/`submit_flushes_` (derived O(1) summaries for quiescence and evidence — not semantic authority), the delivery records with `event_owed`/`owed_key` (the same B1-B control-ref pairing for the zero-op's owed ready event). Compatibility residue: `Completion`'s arena binding flavor, `install_binding`/`rollback_claim_before_accept` and `detail::submit_transaction` now have zero production consumers and are removed with the compatibility Completion itself under #395/#402; `RequestHandleState::backend_ready` is no longer produced by any backend. Temporary: `adopt_request_core` remains a seam only while both backends exist beside unmigrated paths — with both backends migrated it collapses into the backend interface contract at B1-D |
| Status | io_uring production admission/lifecycle: the B1-C scope above is VERIFIED for this record's PR head under the recorded configurations (deterministic interleaving suite, mutation kills, asanubsan + TSan on the self-contained targets, full debug/release matrix, no-liburing build, real-kernel runs, stage-B1-1 TLC rerun with mutations). Both production backends now use the context-owned RequestCore as their single semantic lifecycle authority; the assessment row's remaining GAP content is the #400/#401 boundary recorded above. Open and explicitly not claimed: the public `Request<T>` API (#395), Observer (#396), ProgressSource/no-lost-wake (#397), RequestScope (#398), host redesign (#399), the full cancel/effect profile (#400), the shutdown/poison state machine (#401), and compatibility-surface retirement (#395/#402) |

### B1-C mechanism justification (AGENTS.md gate)

Every meaningful runtime mechanism this slice adds or retains on the io_uring
path, against the gate's questions.

| Mechanism | Fact / owning authority | Justification | Derived? | Classification | Replaces; removal condition |
|---|---|---|---|---|---|
| Monotonic cookie allocator (`next_cookie_`, control tag) | The kernel-visible transport identity of one prepared operation (owner: the backend) | A late CQE from an old kernel operation must never target a reused request: cookies never repeat and never enter the tagged control range; distinct from core generation safety and proved separately by the stale-cookie case and `COOKIE_REUSE` | No | Authority (transport identity) | Replaces nothing (pre-existing, presumed load-bearing, confirmed); removal only with the io_uring transport itself |
| `RouterEntry {cookie, handle, control_state, terminal_delivered, in_use}` | Which live kernel operation maps to which core request, and which of its two CQE classes have retired (owner: the backend) | A control CQE must find its target after the original CQE and vice versa; the entry must outlive both retirements when a cancel is in flight | `terminal_delivered` is an independent transport fact (the core's public surface cannot express "terminal offered but control owed"); `handle` mirrors the core's key by value — routing payload, not a second authority | Transport state | Replaces the old `deferred_terminal` pair; removable only when io_uring cancellations cease to be transport-visible |
| `TransportLedger` (FIFO, kind, cookie, handle, physical position, Class-A flag) | The prepared-vs-submitted boundary of every SQE handed toward the kernel, in submission order (owner: the backend) | BACKEND-03: a negative submit return must not be assumed to prove invisibility; recovery and accounting need the exact ordered obligation set; bounded by SQ depth | No | Authority (transport accounting) | None; removed only with the ring |
| Bounded dispatch ring | Accepted-undispatched request handles (owner: the backend) | REQ-02: accepted-but-undispatched work stays discoverable and bounded, never orphaned by queue pressure | No | Transport state | None |
| `publication_pending_` (`BoundedDispatchQueue`) | The backend's bounded progress worklist of migrated io_uring requests for which the physical/core conditions to drive compatibility publication are already produced (terminal chosen at `offer_terminal`, borrow-touching ref retired at `release_execution`) but publication is not yet serviced (owner: the `UringAsyncBackend` progress mechanism); it is not the authority for whether a request is terminal or publishable | CQE/transport processing needs a bounded way to queue publication work for later progress servicing without performing compatibility publication while holding the transport/dispatch lock; bounded by request capacity because each entry is the slot handle of an accepted-but-unpublished request | Membership is derived from core facts already fixed when an entry is queued, but materialized because the backend progress driver (`poll`/`wait_one`) needs an explicit bounded worklist to publish outside the transport lock; semantic publication eligibility stays derivable/validated through `RequestCore` (`begin_publication` remains the grant), so the queue is not canonical lifecycle state | Derived physical/progress worklist; non-authoritative; bounded by request capacity | For the migrated io_uring path, replaces the removed `RequestArena` ready ring — a mechanism replacement, not a transfer of lifecycle authority: terminal arbitration, canonical result, publication eligibility, public request identity, public binding lifetime and slot reclaim remain `RequestCore` authority. Retained while the current compatibility publication/delivery path requires a backend worklist; removable only when the downstream Observer/Progress delivery migration (#396/#397 or its eventual authorized replacement) removes that consumer and equivalent progress responsibility exists elsewhere — not merely because B1-D begins |
| `PreparedUringOp` per slot | The physical SQE parameters (fd, buffer, length, native length, offset) (owner: the backend) | The core stores borrow facts, not SQE lowering; the SQE needs the kernel-length clamp | No | Transport state | None |
| `DeliveryRecord {completion, publish, waiter fields, event_owed, owed_key}` | The compatibility publication target and the owed ready event paired with one core control ref (owner: the compatibility delivery path) | The zero-op publishes at acceptance while its ready event must still be delivered; the pairing is the B1-B corrective carried to io_uring | The record mirrors the core's binding by pointer/key for delivery only | Compatibility state + physical delivery obligation | Removed with the compatibility `Completion`/ready-event surface under #396/#395/#402 |
| `event_owed` ⇄ `acquire_control`/`release_control` pairing | "One ready event is owed and pins its slot" (owner: the core's `control_refs`) | A real deferred delivery obligation must participate in core reclaim accounting (V26) | No: the flag is the delivery-path half of the core ref | Physical obligation (compatibility) | None; same shape B1-B introduced |
| `live_cookies_` / `live_control_sqes_` / `submit_flushes_` | O(1) counts of live router entries, submitted-unretired control SQEs, submit flushes (owner: the backend) | Quiescence and wait-loop decisions must not scan; evidence counters for the suite | Yes — exact mirrors of router/ledger contents, recomputed nowhere else | Derived summary (non-authoritative) | None; retained for bounded operation |
| Poison recovery state (`fatal_error_` + `note_health_failure` + Class-A ledger walk) | Ring transport health and the per-entry recovery retirement facts (owner: the backend; admission closure owned by the core) | #16's minimum: a fatal ring must not strand or orphan accepted work; kernel-invisible work converges, kernel-visible work keeps its real completion path | `fatal_error_` is the transport health fact; admission closure is derived through the core call | Backend transport health + recovery bookkeeping | Full shutdown/poison refinement is #401; the fabricated-terminal-for-prepared-entries assumption is recorded there |
| `Submission Epilogue` gates / `inject_cqe_for_test` / fake submit / stage injections (all `SLUICE_ASYNC_INTERNAL_TESTING`) | Deterministic ordering and fault evidence the kernel cannot schedule on demand (owner: the test seam) | #19–#21's evidence classes; clearly non-production-authoritative | No | Test-only evidence | Withdrawn with the test targets that use them |

## B1-D review record — Issue #394, cross-backend evidence convergence and seam contraction

This record closes #394's B1 migration scope, in the frozen internal order:
evidence convergence, authority correspondence audit, justified contraction,
ledger closure. No production semantic transition changed in this slice: after
B1-C (base `fb59da8b`, PR #423 ancestor) both production backends already ran
under the context-owned `RequestCore`. B1-D removes the migration seam that
made the authority transferable, converges the recorded evidence, and closes
the ledger statements that still described a half-migrated repository. The
public `Request<T>` API, observer, progress, request scope, host redesign,
cancel/effect profile, shutdown state machine and compatibility retirement
belong to #395–#401 and are not claimed here. This record is not a v1 final
verdict; the assessment rows keep their statuses.

| Field | Content |
|---|---|
| Requirement scope | ARCH-01/ARCH-02 finalized (the context owns the core; a backend must not own it or allocate public identity — the adoption seam that made this negotiable is gone); REQ-01/REQ-02/REQ-03/REQ-04/REQ-05/BOUND-01/BOUND-02 production halves for both backends (evidence convergence only, no transition changed); VERIFY-04 V04/V05/V09/V24/V26 converged reruns; HANDLE-03 basis. Excludes everything owned by #395–#401, and any behavior change: this slice changes mechanism shape, not behavior |
| Implementation | `AsyncBackend` loses the `adopt_context_identity`/`adopt_request_core` private virtuals (and the default returning false) and gains `slot_capacity()`, a pure private virtual, plus a protected `core_` pointer installed exactly once by the `AsyncIoContext` constructor after it allocates the core with the fresh identity and the reported capacity. `ThreadPoolBackend` and the liburing-enabled `UringAsyncBackend` delete their seam overrides and their derived `core_` mirrors — the inherited member is the single representation of "the core this backend serves" — keep a `slot_capacity()` override, and rename the internal-testing accessor `adopted_core_for_test` → `request_core_for_test`. The no-liburing `UringAsyncBackend` declaration gains no `slot_capacity()` override and therefore remains abstract, as it already was at the B1-D base (`fb59da8b`), where its pure `adopt_context_identity` override was likewise present only in the `SLUICE_HAS_LIBURING` configuration. B1-D therefore introduces no no-liburing source-behavior change. The ownership evidence target's seam case becomes `capacity_handshake_happens_once_at_context_construction` (zero capacity queries at backend construction, exactly one at context construction, none on a context move). Also removed: `UringAsyncBackend::queue_depth_`, a write-only config mirror (constructor-initialized, zero readers at base) that blocked clang `-Werror` sanitizer builds of the uring TU |
| Change | No public API or behavior change. Machinery strictly reduced: 9 files, +44/−82. The context→backend handshake is now "one capacity query + one core install" expressed in the backend interface contract; no backend can exist without the context learning its slot-table capacity (pure virtual), and no backend can hold a second core representation. `UringConfig::queue_depth` remains the ring-setup authority |
| Semantic/regression evidence | Newly executed on this branch, all against the final head. gcc debug `--liburing=y`: 44/44 test binaries — ownership 8/8 and uring ownership 9/9 (real ring), protocol 34/34, publication 11/11, consumer probe 2/2, ThreadPool cutover 14/14, uring cutover 20/20 (deterministic seam + real-kernel cases), uring smoke 8/8 (real ring), `semantic_validation_precedence_test` 21 direct compared / ThreadPool 19+2 / io_uring 19+2 / 0 divergences, `semantic_backend_conformance_test` 0 divergences, admission 4/4, file access precedence 40/40, submit boundary 2/2, uring public consumer probe, 30-suite remainder; the eight B1-C mutant binaries rebuilt with every mutation detected (same named failing cases as B1-C; `PUBLISH_BEFORE_RETIREMENT` via the core's protocol-unreachable diagnostic). gcc release `--liburing=y` 44/44. gcc `--liburing=n` 39/39 — the no-liburing library/configuration builds and passes its registered suite; no claim is made that `UringAsyncBackend` is instantiable in this configuration (the uring binaries in the tree were verified stale by timestamp so no stale-archive pass is claimed). asanubsan (clang 21.1.8) 11/11 including the library-linked admission/precedence/semantic targets that gcc cannot build in this mode. tsan 4/4 (both ownership suites, both cutover suites). `git diff --check` clean; `scripts/check_cpp_comment_authority.py` clean. Reused unchanged (inputs untouched): the stage-B1-1 TLC campaign stands as rerun at B1-C (6,165,549 states generated / 996,436 distinct, safety clean, `PROPERTY L1 L5` clean, 14 safety + 3 temporal mutants killed, subsumption witness clean) because `formal/tla/RequestCore.tla` and its C++ correspondence are unchanged — the seam was never a modeled transition (B1-A recorded it as migration surface); B1-B's delivery-pin mutation kills (the mutated ThreadPool paths are unchanged); B1-A's identity-domain mutation kills (`context_identity.cpp` untouched). Not rerun, deliberately: the TLC campaign (no model or correspondence change), and gcc sanitizer coverage of fiber-linked targets (the pre-existing `fiber_ctx.cpp` attribute blocker recorded since A2 — unchanged; clang asanubsan covers the full library including that TU) |
| Protocol/model evidence | Model unchanged; the B1-B/B1-C correspondence tables are inherited verbatim (Reserve/Rollback/Accept/ClaimExec/PhysicalOutcome/CancelRequest/RetireExec/AcquireCtl/ReleaseCtl/RetireCtl/BeginPublish/CompletePublish/ReleaseBind/Reclaim/CloseAdmission/NoteHealth all map to the same core API the backends call identically). The contraction removes no C++ transition any modeled action maps to |
| Publication/thread evidence | No new lock, atomic or edge. The base `core_` member is installed in the context constructor before the context is reachable by any other thread and is immutable thereafter; the destructor retires the backend before the core storage dies (the unchanged B1-B/B1-C order), so no legal interleaving observes a dangling `core_`. The single capacity query happens under the same construction discipline. TSan reruns above |
| Backend/kernel evidence | Real kernel ring (liburing 2.14, kernel 6.18.33, WSL2; tmpfs/ext4 regular files) via the uring smoke, cutover real-kernel cases and the 0-divergence conformance runs; the no-liburing configuration compiled and 39/39. Configurations enumerated in the evidence row |
| Authority-contraction audit | Removed, each with its replacement demonstrated: `adopt_request_core` (seam + default + both overrides) → the protected base `core_` installed by the context constructor — the adoption fact is now structural instead of an overridable negotiation, and no backend can be constructed beside a core it did not receive; `adopt_context_identity` (seam + both overrides + stub) → `slot_capacity()` — its identity half has been dead since B1-B/B1-C removed the arenas the identity was installed into, leaving a capacity-only handshake, and the identity now reaches the core directly at construction; the per-backend derived `core_` members → the single inherited member; `adopted_core_for_test` → renamed `request_core_for_test`; `queue_depth_` → deleted as write-only state (the config field remains the ring-setup authority). Retained, each with an explicit downstream owner: the `Completion` arena binding flavor, `AsyncIoContext::install_binding`, `detail::submit_transaction`, `detail::RequestArena` (with `adopt_context_identity`/`context_`, `identity_handle_state`, the `backend_ready` state and its counters) and `RequestHandleState::backend_ready` — zero production consumers since B1-C, retirement owned by #395/#402; `supports_request_identity`/`resolve_identity_state`/`identity_of` — compatibility identity surface until #395; the `SubmitStage` fault-injection machinery (the only consumer of `submit_transaction.hpp` in backend headers) — test-only evidence for the cutover suites, withdrawn with them; `ApplicationRuntime::admission_closed_snapshot_` — the runtime's own admission-close summary, not a core mirror; the runtime's `issue116-forensics` label `accepted_outstanding` reads the core-derived `outstanding()`, not arena state; every B1-C-classified physical mechanism (cookie allocator, router, `TransportLedger`, `publication_pending_`, delivery records/`event_owed`, prepared ops, dispatch rings) — unchanged classifications; `acquire_control`/`admission_open()`/`health_failed()` substrate surface — unchanged, consumed by both backends and #400/#401 obligations |
| Status | #394's B1 migration scope is closed at this record's PR head: the context-owned `RequestCore` is the single semantic lifecycle authority for every production request on both backends, and the migration seam no longer exists. The remaining obligations named by #394 belong to #395–#401 and are not claimed. The assessment rows above keep their statuses; this record adds no final v1 verdict |

### B1-D mechanism justification (AGENTS.md gate)

The two mechanisms this slice introduces, against the gate's questions.

| Mechanism | Fact / owning authority | Justification | Derived? | Classification | Replaces; removal condition |
|---|---|---|---|---|---|
| `AsyncBackend::slot_capacity()` (pure private virtual) | The capacity of the slot table this backend serves (owner: the backend's physical table) | BOUND-01: the core must be sized by the table it will serve; ARCH-02: the context owns the core, so only the backend can report the budget. Pure virtual so no backend can exist with an unreported table | No | Interface contract (the capacity half of `adopt_context_identity`) | Replaces `adopt_context_identity`; no removal condition within v1 |
| `AsyncBackend::core_` (protected, installed by the context constructor) | Which core this backend serves (owner: the `IoContext`, the sole writer) | ARCH-01/ARCH-02: one context-owned core per backend, reachable by every backend transition; materialized because the backends call the core on every submit/dispatch/CQE/cancel path. Write-once before publication of the context, never cleared; the destructor retires the backend first | No (it is a handle to authority, not a summary) | Authority-owned handle | Replaces `adopt_request_core` and both per-backend derived `core_` mirrors; removed only if backends cease calling the core, which no v1 requirement allows |

## B2 review record — Issue #395, public `Request<T>` settlement responsibility

This record implements the Phase B2 slice of MIG-01: the public result
surface moves onto the context-owned `RequestCore` without a new lifecycle
authority. `Request<T>` is a single-settlement responsibility object over the
core's existing canonical outcome; consumption is atomic with public-binding
ownership; the destructor policy and the context-lifetime precondition are
always-on diagnostics. `Completion` remains the compatibility invocation
form with zero behavior change; its retirement is #402. Observer, progress,
request scope, host redesign, the full cancel/effect matrix and the shutdown
state machine stay with #396–#401 and are not claimed here. This record is
not a v1 final verdict; the assessment rows keep their statuses.

| Field | Content |
|---|---|
| Requirement scope | HANDLE-01 (empty/pending/published/consumed; move-only noexcept transfers; take_result atomic with binding release; try_result non-consuming), HANDLE-02 (destroying/overwriting a known nonterminal Request is an always-on fail-fast, release builds included; a published binding is discarded without waiting for control refs), HANDLE-03 (context destruction with any live public binding forbidden, including ready-but-unconsumed; requests require their context alive), REQ-02/REQ-03 (accepted identity, single canonical terminal), REQ-04 (publication completes without requiring a `Completion<T>*` destination; the new path's HB chain is the core mutex), REQ-05 (reclaim after binding release under the unchanged four-pin predicate), INV-01/INV-02, THREAD-01/THREAD-02 (object-level thread discipline unchanged: one owner, moves synchronize externally), CANCEL-01 routing preservation (core arbitration + backend physical cancel), ERR-01 (pre-accept Result errors, post-accept failures keep the work owned), VERIFY-02 S1–S10 correspondence, VERIFY-04 V05/V08/V14/V21/V24-request-id/V26 + the retained-result §17/§18 evidence. Excludes observer/progress (#396/#397), effect-certainty channels (#400), shutdown (#401), Completion retirement (#402) |
| Implementation | New public header `include/sluice/async/request.hpp`: trivially copyable nonowning `RequestId` (context+slot+generation projection of the core key), `CancelDisposition`, `RequestReadiness`, `RequestObservation<T>`, and move-only `Request<T>` holding exactly `{RequestCore*, AsyncBackend*, RequestKey}` — no mirrored lifecycle booleans, no control block, no shared ownership; `valid()/id()/ready()/try_result()/take_result()/discard()/cancel()`. `RequestCore` gains `observe_public_result` (published-gated, non-mutating), `consume_public_result` (payload copy + binding release + reclaim in one mutex critical section) and `discard_public_result` (published-only binding discard backing the `Request` release path; the compatibility `release_public_binding` stays inflight-tolerant for the legacy Completion ready edge) plus `CoreOccupancy::public_bindings`; two new fail-fast entries (`request_nonterminal_release_fail_fast`, `request_binding_invariant_fail_fast`) back the always-on diagnostics. `AsyncBackend`'s private submit seam is retyped from `Result<void>(Op, Completion<T>&)` to `Result<detail::RequestKey>(Op, Completion<T>*)` — a null completion publishes through the core only (no-op `publish_request_ready` thunk, nullable `DeliveryRecord::completion`) — and gains the private virtual `cancel_identity(RequestKey)` that routes into each backend's existing `cancel_key` arbitration. `AsyncIoContext` gains the four single-argument submissions returning `Result<Request<std::size_t>>`/`Result<Request<void>>`, `cancel(const RequestId&)` and `lookup(const RequestId&)`; the destructor and move-assign extend the context-outstanding diagnostic with the public-binding check (HANDLE-03). `Request<T>::cancel()` is defined once in `async_io_context.hpp` after `AsyncBackend` is complete. ThreadPool and uring backends share one submit implementation for both invocation forms; the no-liburing stub submits return `backend_error` keys and stay abstract (`cancel_identity` overridden only in the liburing configuration, matching the B1-D precedent) |
| Change | Public API addition plus a seam retyping; the compat `Completion` path's observable behavior is unchanged (the same core calls run with a non-null completion pointer). The backends' four submit overrides per backend collapse from completion-only to shared identity-returning forms; `unsupported_stub()` in the no-liburing uring TU lost its last reader to the retyping and is deleted. The new-path publication carries no `Completion` ready-atomic involvement: offer_terminal → begin/complete_publication → consumer's core-mutex acquire is the whole chain. Review-fix round: `Request`'s destructor/discard/move-assign-over release path moved from the compatibility `release_public_binding` (which must stay inflight-tolerant because a ready Completion's own release edge precedes the core's publication completion) to the published-only `discard_public_result`, so a `publication_inflight` slot is again a nonterminal for the public surface and the release fail-fasts; the Completion path's observable behavior is untouched |
| Semantic/regression evidence | Executable/model evidence is recorded per head, not collapsed into one "final head". The configuration battery below ran at head `29df935d` (the review-fix round re-ran every configuration after the fix commits `706400eb`/`8a95fc32`/`29df935d`); the only intervening delta to the PR head that the fix-round re-review examined, `2952f75d`, is documentation/formal-map ledger editing with no C++, model or test executable source change. The V14 short-write case below is the sole executable evidence added since `29df935d`; it was executed at its introducing commit on this branch in the configurations stated there. gcc debug `--liburing=y`: 48/48 registered tests — public request 13/13 on the ThreadPool build and 13/13 on the real-kernel io_uring build (the new `publication_inflight_window_keeps_the_request_nonterminal` case pins the window semantics deterministically on both backends), release-violation death suites 4/4 on both backends (destructor, move-assign-over, context-with-live-binding, and the new `discard_request_during_publication_inflight` which asserts `ready() == false` under the epilogue pause gate and then requires the always-on `SIGABRT`), ownership 8/8 + uring ownership 9/9 (both tracer-bullet cases migrated to the new API), protocol 34/34, publication 11/11, cutover 14/14 + 20/20 (seam + real-kernel), uring smoke, semantic precedence/conformance 0 divergences, admission 4/4, file-access precedence 40/40, 30-suite remainder. gcc release `--liburing=y` 48/48 — the death evidence therefore demonstrates the always-on diagnostics terminate in release builds (fork/exec death tests assert `SIGABRT`, never a debug-only assertion). gcc debug `--liburing=n` 41/41 and gcc release `--liburing=n` 41/41 (the two B2 uring-variant registrations are liburing-gated; the no-liburing library builds with the retyped abstract stub). asanubsan (clang) 48/48 full registered suite including both death suites. TSan 8/8 focused — public request ×2, death ×2, ownership ×2, cutover ×2 — zero reports (an unsynchronized shared read buffer between the two contexts of the move-over death scenario, latent since B2's first tests and surfaced by TSan timing on this round, was fixed by giving the pending submission its own buffer). Requirement-facet evidence naming (VERIFY-04): V21/HANDLE-03 = `public_request_release_violation_test` / `public_request_release_violation_uring_test` scenario `destroy_context_with_live_public_binding` (both backends, debug and release); V14 = `uring_core_cutover_test` case `cancel_racing_short_write_keeps_confirmed_count` (liburing=y, io_uring seam backend, injected CQEs: a `read_write` fixture takes a `submit_write` of 6 bytes, cancel intent is recorded on the running write, the physical completion then arrives as a successful short write of 3/6 — the slot observation pins the canonical terminal as `succeeded` with `confirmed_bytes == 3` over the racing cancel intent, the publication carries 3 with no canceled replacement and no zero-effect rollback fabricated, and the cancel control CQE then retires with full reclaim; 21/21 uring cutover cases pass with it in gcc release and gcc debug `--liburing=y`), with the earlier `cancel_racing_completion_keeps_the_confirmed_count` retained as the successful short-read flank of the same race (companion evidence; this record's earlier claim that that read case proved a successful short write was wrong and is withdrawn), backed by the backend-independent outcome oracle `semantic_effect_outcome_test` case `case_c_canceled_keeps_confirmed_bytes` (`canceled_racing_in_flight_attempt(9)` keeps `confirmed_bytes == 9` with `remaining == unknown`) and `prefix_only_terminal_cannot_carry_v15` (a racing-cancel outcome cannot carry a prefix-only terminal); the B2 pre-dispatch cancel case (`cancel_wins_before_dispatch_and_converges`) is CANCEL-01 floor evidence, not V14. Six B2 mutant binaries each killed by its named evidence (table below). `git diff --check` clean; `scripts/check_cpp_comment_authority.py` clean. Reused unchanged: the B1-B/B1-C delivery-pin and identity mutation kills (those paths are behaviorally untouched for non-null completions), B1-A identity-domain kills, the B1-C named mutation campaign (its machinery and binaries are unchanged) |
| Protocol/model evidence | `formal/tla/RequestCore.tla` extends B1-1 with the missing public-consumption distinctions only: action `Consume(s)` (bind live, terminal chosen, publication complete — `pub = "done"`, never inflight; atomically records the consumed ghost, releases the binding, promotes stage 3; consumer-driven, deliberately outside `Fair`), ghosts `consumed[i]` and `pubDoneIds`, invariants `InvConsumeAfterPublish` (consume presupposes this identity's completed publication — expressible only through the per-identity `pubDoneIds` fact because consume promotes stage past 2 in the same step) and `InvConsumedReleased`, plus mutant switches `MutConsumeKeepsBind` and `MutConsumeUnpublished`. Review-fix round: the model gained the public-discard distinction the C++ release-path fix required — action `DiscardPublic(s)` (public identity resolution with live binding and chosen terminal; guarded by `pub = "done"` unless mutant; atomically records the ghost, releases the binding, promotes stage 3 — consumer-driven, outside `Fair`), ghost `discarded[i]`, invariant `InvDiscardAfterPublish` (`discarded[i] => i ∈ pubDoneIds`, per-identity for the same stage-jump reason as consume), and mutant switch `MutDiscardInflight` (relaxes the gate to `pub ≠ "none"`); `ReleaseBind` remains deliberately inflight-tolerant as the compatibility release flavor (`Completion::release_binding_` → `release_public_binding`). All 22 cfgs carry the new constant; `RequestCore.cfg`/`RequestCoreLive.cfg` add `InvDiscardAfterPublish`; `RequestCoreMutDiscardInflight.cfg` joins the B2 stage; `scripts/verify_tla.sh` gains `rcore-mut-discard-inflight`. TLC rerun on the review-fix head (tla2tools v1.7.4, OpenJDK 25.0.4.1; the complete `scripts/verify_tla.sh` gate — legacy stages, rcore safety/liveness/mutant battery/witness, stage B2 — rerun end-to-end on a 20-core x86-64 Linux host with `VERIFY_TLA: PASS` and zero FAILs): safety clean (153,347,909 states generated / 26,662,372 distinct / 0 on queue, `Model checking completed. No error has been found.` — the discard branching grows the explored space ≈4.1× over the first B2 record) and liveness clean (`PROPERTY L1 L5` on the identical 153,347,909/26,662,372/0-on-queue graph, `Model checking completed. No error has been found.`; the 12 temporal-property branches checked during the run, final temporal pass 1h17m, rcore-live ≈18.5h wall on the grown graph) — the B1-1 stage facts (safety, L1/L5, 14 invariant mutants, 3 temporal mutants, subsumption witness) re-verified against the extended model; the three B2 mutants are killed on their named invariants (`MutConsumeKeepsBind` → `Invariant InvReleasedNotLive is violated`, 2,395 states; `MutConsumeUnpublished` → `Invariant InvConsumeAfterPublish is violated`, 91 states; `MutDiscardInflight` → `Invariant InvDiscardAfterPublish is violated`, 938 states — each count identical in a local pre-push run and in the gate). Consume correspondence: `Request::take_result` ≙ Consume (the `resolve_public_`/published gate is the action guard; the payload copy + `binding_live = false` + `try_reclaim_` is the atomic effect); `Request::try_result` ≙ the observation reading of the same state (no modeled action — observation does not change protocol state, and the stale→fail-fast mapping is the C++ projection of the identity-resolution guard shared with the release ops); `Request::discard`/destructor/move-assign ≙ `DiscardPublic` (published-only — the first record wrongly mapped this path to `ReleaseBind`, which is the inflight-tolerant compatibility flavor the legacy Completion release uses); `ctx.lookup` ≙ `lookup` (no action) |
| Publication/thread evidence | The new consumption path adds no lock, atomic or edge: `observe/consume_public_result` run under the core mutex, the same serializer that publishes, so a consumer that acquires the mutex after `complete_publication` reads the canonical outcome with acquire visibility — the B1 HB chain with the Completion ready-atomic removed from the new path entirely (the no-op thunk performs no user-visible write). The Request object itself follows THREAD-01: single-owner, move across threads with external synchronization, no concurrent member is data-race-free by fiat. The `cancel_identity` virtual is internally synchronized (backend dispatch/work mutex + core mutex) and never touches delivery records, so `Request::cancel` needs no context mutex and never blocks behind submit serialization. `AsyncBackend*` and `RequestCore*` inside a Request remain address-stable across context moves (the B1-A uniqueness discipline: the context's `unique_ptr`s are moved, never copied, and the pointees never relocate). Publication-window discipline (exercised deterministically by the new death scenario and the window case): while the progress owner is parked between `record.publish` and `complete_publication` it holds the context's `access_mtx_` inside `AsyncIoContext::poll`, so context-mediated probes (submit/poll/wait/`lookup`) from the user thread block until publication completes, whereas the object-level probes `ready()`/`try_result()` and the release decision touch only the core mutex, which the publisher does not hold mid-publication — the interleaving the HANDLE-01/HANDLE-02 release gate must judge. TSan runs above |
| Backend/kernel evidence | Real kernel ring (liburing 2.14, kernel 6.18.33, WSL2) for the uring public-request suite (13/13) and death suite (4/4), including the new publication-window discard scenario, the gate-window cancel win and the zero-op consumed-pin case on the live backend; the no-liburing configuration builds with the retyped stub. Configurations enumerated in the evidence row |
| Authority-contraction audit | Mechanism replacements: the `Result<void>` submit seam → the key-returning pointer form (the accepted identity now flows to the caller on every submission, shared by both invocation forms — this is what makes "post-accept failure must not hide accepted work" representable rather than invisible); the implicit "publication destination is always a `Completion`" coupling → nullable `DeliveryRecord::completion` + the no-op `publish_request_ready` thunk. Deleted: `unsupported_stub()` (last reader removed by the retyping). Redundancy check against pre-B2 mechanisms: nothing that B1 classified became redundant — the core's binding/terminal/publication/reclaim facts are exactly the authorities the new surface reads, `ReleaseBind` remains consumed by the legacy Completion compatibility release (`Completion::release_binding_`), distinct from Consume's stricter published-complete gate — the Request discard/destructor/move-assign path uses `DiscardPublic`/`discard_public_result`, not `ReleaseBind` — `lookup`/`cancel` arbitration are reused unchanged, and the B1-C physical mechanisms (cookie allocator, router, ledger, publication worklist, delivery records) keep their classifications. Compatibility residue unchanged in status, each with zero NEW consumers added by this slice: the `Completion` arena binding flavor, `AsyncIoContext::install_binding`, `detail::submit_transaction`, `detail::RequestArena` (with the `backend_ready` state — zero producers since B1-C) and `RequestHandleState::backend_ready` — retirement owned by #402 (the compat invocation form itself is deliberately retained there); `supports_request_identity`/`resolve_identity_state`/`identity_of` remain compat-only RequestHandle surface, with the new API reading the core directly so no second identity authority exists. Review-fix round: no mechanism became redundant and none was deleted; `release_public_binding` lost its only Request-path consumer and is now consumed solely by the compatibility Completion release (`Completion::release_binding_`), which is what justifies keeping its inflight-tolerant precondition; `discard_public_result` is a narrower precondition over the same core-owned publication facts — no new state, flag or counter, no second authority |
| Status | The #395 Phase B2 implementation slice is complete, and the independent final review of the first record returned REQUEST_CHANGES with one merge-blocking defect (public discard accepted a `publication_inflight` slot) and one evidence-naming gap (V14/V21); the review-fix round at head `29df935d` resolved the implementation defect — the published-only `discard_public_result` backs the Request release path with a deterministic publication-window death scenario on both production backends, release builds included, a named mutant killed by exactly that scenario, and the model split into `DiscardPublic` (published-only) versus `ReleaseBind` (compatibility, inflight-tolerant) with its mutant killed — and named V21 precisely. The re-review of that round returned CONDITIONAL_PASS with two P2 evidence/closure items, both addressed by this record: the V14 citation had described a short-read case while the root requirement names a successful short write (V14 now names the deterministic short-write race `cancel_racing_short_write_keeps_confirmed_count`, executed in release and debug), and the evidence records had conflated the executable-evidence head `29df935d` with the reviewed PR head `2952f75d` (now recorded per head with the intervening docs-only delta stated). Not claimed: observer/progress (#396/#397), cancel/effect matrix and backend refinement (#400), shutdown (#401), Completion and legacy retirement (#402). The assessment rows keep their NOT_ASSESSED-as-a-whole statuses; the narrow closure re-check has not run, the PR is not merged and #395 is not closed |

### B2 mechanism justification (AGENTS.md gate)

Every new runtime mechanism of this slice, against the gate's questions.

| Mechanism | Fact / owning authority | Justification | Derived? | Classification | Replaces; removal condition |
|---|---|---|---|---|---|
| `RequestCore::observe_public_result` | Whether this identity's terminal is published, and the canonical payload if so (owner: the core) | HANDLE-01 requires a non-consuming ready probe; REQ-04 requires observation behind the publication handoff | No — a public read operation on the authority | Authority operation | None; part of the core's public contract for v1 |
| `RequestCore::consume_public_result` | One-time settlement: payload acquisition + public-binding release + reclaim eligibility (owner: the core, one mutex section) | HANDLE-01 requires consumption atomic with binding ownership so no interleaving can observe consumed-but-bound or bound-after-consumed; REQ-05 reclaims under the unchanged predicate | No | Authority operation | Replaces nothing; public Request discard uses `discard_public_result`, while `release_public_binding` remains compatibility-only for legacy Completion; no removal condition within v1 |
| `CoreOccupancy::public_bindings` | Count of live public bindings (owner: the core's `binding_live` facts) | HANDLE-03's destruction precondition must be checkable at context destruction; materialized because destruction is the only moment the obligation is enforced | Yes — derived summary over `binding_live` | Derived summary consumed by a contract diagnostic | Extends the existing context-outstanding check; removed only with the HANDLE-03 destruction precondition itself |
| `RequestId` | The public projection of the core identity (context, slot, generation), valid while the binding is live (owner: the core) | The root's copied-identity lookup/cancel semantics (stale/not_found after release) require an owning-handle-free identity value | Yes — a projection of `RequestKey` | Public value view of core authority | Replaces nothing (`RequestHandle`'s identity surface is compat); no removal condition within v1 |
| `Request<T>` | The single public settlement responsibility: empty or bound, one terminal consumption (owner: the core holds every fact; the object holds only the handle triple) | HANDLE-01/02: the root's public handle model — move-only, noexcept transfers, destructor policy, explicit discard. No mirrored booleans: every lifecycle answer is a core query | No (handle, not summary) | Authority handle | Replaces nothing yet; per-consumer `Completion` migration is #402. Removal: never within v1 (it is the product surface this slice exists to deliver) |
| `AsyncBackend::cancel_identity` (private virtual) | Cancel-by-identity routing into the backend's existing arbitration (owner: each backend's `cancel_key`) | CANCEL-01 preserved: the Request path must reach the same core arbitration + backend physical cancel without owning a `Completion`; internally synchronized so cancel never blocks behind submit serialization | No — routing, not state | Interface routing seam | Replaces nothing; removable only if identity-based cancel gains a non-virtual path, which no v1 mechanism provides |
| `publish_request_ready` no-op thunk + nullable `DeliveryRecord::completion` | Publication without a `Completion` destination (owner: the backend's delivery record) | REQ-04: the publication path must complete without requiring a `Completion<T>*`; the new path's HB proof must not route through Completion's ready atomic | The thunk is the null-destination publication effect; the nullability is a physical record field | Physical/compat decoupling | Replaces the implicit completion-only publication coupling; removed with the delivery records when #402 retires the compat publication path |
| `request_nonterminal_release_fail_fast` / `request_binding_invariant_fail_fast` | HANDLE-02 violation (destroying/overwriting a nonterminal responsibility) and the protocol-unreachable stale observation (owner: the contract, enforced at the Request surface) | Root-required always-on diagnostics — release builds included; not defensive insurance (the states they catch are reachable misuse, and the stale arm is the fail-stop for identity misuse the root forbids silently absorbing) | No | Contract diagnostics | None; stand only while the Request surface stands |
| Model `Consume(s)` + `consumed`/`pubDoneIds` ghosts | The consumption transition and its per-identity published presupposition (owner: the model's correspondence to the core op) | The B1-1 model could not express consume-before-publish (stage jumps past 2 in the same step), so `pubDoneIds` is the minimal per-identity fact that makes the presupposition statable and mutable | The ghosts monitor the modeled transition | Test/formal evidence (model-internal) | None; lives with the model |
| `RequestCore::discard_public_result` | Whether this identity's public terminal is established, and the binding discard that follows (owner: the core's `published`/`binding_live` facts) | HANDLE-01/02 require the public Request release to judge only completed publication: the compatibility release's inflight tolerance exists for the Completion ready edge and must not govern the public surface; a narrower precondition over the same facts, no new state | No — a public release operation on the authority | Authority operation (published-only flavor, distinct from the compatibility flavor) | Nothing replaced; `release_public_binding` remains for the compat Completion release. Removed only if the two release flavors ever collapse, which #402 owns |
| Model `DiscardPublic(s)` + `discarded` ghost + `InvDiscardAfterPublish` | The published-only public-discard transition and its per-identity presupposition (owner: the model's correspondence to `discard_public_result`) | The single `ReleaseBind` made the model looser than the C++ contract for the Request path; splitting the flavor keeps each release edge's guard truthful and gives `MutDiscardInflight` a killer | The ghost monitors the modeled transition | Test/formal evidence (model-internal) | None; lives with the model |

### B2 mutation evidence (C++ minimum discriminating set)

| Mutant define | Flipped mechanism | Killer (named, on the mutated build) |
|---|---|---|
| `SLUICE_B2_MUTANT_CONSUME_KEEPS_BINDING` | `consume_public_result` returns the payload without releasing the binding or reclaiming | `public_request_test` `accepted_request_pends_until_publication`: "consumption released the binding and reclaimed", "the consumed identity stops resolving", "cancel by the consumed identity reports not_found" |
| `SLUICE_B2_MUTANT_OBSERVE_IGNORES_PUBLICATION` | `observe_public_result` skips the published gate | `public_request_test` `accepted_request_pends_until_publication`: "try_result reports pending before publication" |
| `SLUICE_B2_MUTANT_OBSERVE_CONSUMES` | `observe_public_result` releases the live binding | `public_request_test` `accepted_request_pends_until_publication`: the always-on stale-binding diagnostic terminates the process deterministically (the worker's final control release reclaims a slot the mutant unbound early); the same fault is discriminated by the retained-binding assertion "observing does not release the retained public binding" |
| `SLUICE_B2_MUTANT_RELEASE_FORGETS_BINDING` | `Request::release_responsibility_` nulls the handle without releasing the binding | `public_request_test` `discard_releases_the_published_binding`: "discarding released the binding and reclaimed", "the discarded slot admits new work" |
| `SLUICE_B2_MUTANT_NONTERMINAL_RELEASE_DETACHES` | The nonterminal release arm loses its fail-fast (treats `not_visible_yet` as released) | `public_request_release_violation_test` `destroy_nonterminal_request`: "the always-on diagnostic did not terminate" |
| `SLUICE_B2_MUTANT_DISCARD_ACCEPTS_INFLIGHT` | `discard_public_result` treats `publication_inflight` as published (the exact defect the review-fix round closes) | `public_request_release_violation_test` `discard_request_during_publication_inflight` (both backend builds carry the scenario; the mutant build is the ThreadPool seam configuration): "the always-on diagnostic did not terminate" — the scenario's `ready() == false` assertion under the epilogue pause gate proves the window is entered, and the strict gate SIGABRTs where the mutant releases silently |
