\* Reachability witness (reverse-invariant): the admission-expire half of the
\* timed precedence genuinely fires — NOT resource-admissible + already-due
\* deadline resolves Expired at admission (audit #162 MODEL-003).
\* Expected: Invariant NoReachUntilExpired is VIOLATED (CEX = witness).
CHECK_DEADLOCK FALSE

CONSTANTS
    Epochs = {E1, E2, E3}
    E1 = E1
    E2 = E2
    E3 = E3

SPECIFICATION Spec

INVARIANT NoReachUntilExpired
