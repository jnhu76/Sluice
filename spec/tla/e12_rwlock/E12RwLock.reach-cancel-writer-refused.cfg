\* Reachability witness (reverse-invariant): cancel exposes a writer head that
\* REMAINS blocked while active readers stay (R1 topology; the
\* activeReaders = 0 refusal branch of the cancel reconcile).
\* Expected: Invariant NoReachCancelWriterRefused is VIOLATED (CEX = witness).
CHECK_DEADLOCK FALSE

CONSTANTS
    Epochs = {E1, E2, E3}
    E1 = E1
    E2 = E2
    E3 = E3

SPECIFICATION Spec

INVARIANT NoReachCancelWriterRefused