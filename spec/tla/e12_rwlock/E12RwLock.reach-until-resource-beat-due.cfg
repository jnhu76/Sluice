\* Reachability witness (reverse-invariant): timed admission where the
\* resource was admissible AND the deadline already due resolved Woken —
\* precedence 1 genuinely fires with due = TRUE (audit #162 MODEL-003).
\* Expected: Invariant NoReachUntilResourceBeatDue is VIOLATED (CEX = witness).
CHECK_DEADLOCK FALSE

CONSTANTS
    Epochs = {E1, E2, E3}
    E1 = E1
    E2 = E2
    E3 = E3

SPECIFICATION Spec

INVARIANT NoReachUntilResourceBeatDue
