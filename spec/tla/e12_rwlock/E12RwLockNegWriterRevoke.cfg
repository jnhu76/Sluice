\* NEG-RW3 (writer-reconcile drops the activeReaders = 0 guard; reader
\* revocation): must VIOLATE ReaderRevocationFree — a head writer granted while
\* a live reader still holds silently revokes it (audit #162 MODEL-002 negative
\* control). All other laws remain intact so the named check is exact.
CHECK_DEADLOCK FALSE

CONSTANTS
    Epochs = {E1, E2, E3}
    E1 = E1
    E2 = E2
    E3 = E3

SPECIFICATION Spec

INVARIANT ReaderRevocationFree