\* Reachability witness (reverse-invariant): expire exposes a writer head that
\* REMAINS blocked while active readers stay (R2 topology; the
\* activeReaders = 0 refusal branch of the expire reconcile).
\* Expected: Invariant NoReachExpireWriterRefused is VIOLATED (CEX = witness).
CHECK_DEADLOCK FALSE

CONSTANTS
    Epochs = {E1, E2, E3}
    E1 = E1
    E2 = E2
    E3 = E3

SPECIFICATION Spec

INVARIANT NoReachExpireWriterRefused