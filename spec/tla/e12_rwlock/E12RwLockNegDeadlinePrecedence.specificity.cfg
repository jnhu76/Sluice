\* NEG-RW4 SPECIFICITY gate: the SAME mutant that must violate
\* InvResourceFirstDeadline (cfg E12RwLockNegDeadlinePrecedence.cfg) must
\* PASS every remaining positive invariant — proving the negative is EXACT:
\* it fails for the deadline-precedence defect and nothing else (no
\* collateral damage such as an Expired node holding the writer lock, which
\* would violate WriterOwnerConsistency). Audit #162 MODEL-003 negative
\* control, PR #168 review closeout.
CHECK_DEADLOCK FALSE

CONSTANTS
    Epochs = {E1, E2, E3}
    E1 = E1
    E2 = E2
    E3 = E3

SPECIFICATION Spec

INVARIANTS
    MutualExclusion
    NoReaderWriterOverlap
    NoReaderBarging
    ReaderBatchCorrectness
    TerminalUniqueness
    PublicationUniqueness
    NoLinkedTerminal
    ActiveReaderIntegrity
    QueueNoDuplicates
    WriterOwnerConsistency
    InvNoStrandedGrantableHead
    ReaderRevocationFree
