\* NEG-RW4 (deadline precedence inversion): must VIOLATE
\* InvResourceFirstDeadline — a *_lock_until admission that saw BOTH an
\* admissible resource and an already-due deadline resolves Expired instead
\* of Woken, committing no ownership (the evidence latches stay TRUE, so the
\* inversion is detectable; audit #162 MODEL-003 negative control). The
\* mutant's exactness is proven separately: the sibling
\* E12RwLockNegDeadlinePrecedence.specificity.cfg checks the SAME mutant
\* against the remaining 12 positive invariants and must PASS.
CHECK_DEADLOCK FALSE

CONSTANTS
    Epochs = {E1, E2, E3}
    E1 = E1
    E2 = E2
    E3 = E3

SPECIFICATION Spec

INVARIANT InvResourceFirstDeadline
