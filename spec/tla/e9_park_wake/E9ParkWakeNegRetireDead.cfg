\* #189 negative control — the EXACT pre-fix defect: RetireWorkerQuiescent
\* conjoins BridgeEffect (which primes wakeEpoch'/bridgePending') while its
\* own UNCHANGED pins both variables, so the action is unsatisfiable again
\* and never fires. The non-vacuity witnesses MUST therefore HOLD here
\* (NoReachRetireFired / NoReachQuiescentTerminate PASS): the permanent
\* witness gate fails closed — a reintroduced dead retire is detected.
\* State space is byte-identical to the pre-#189 pristine model (46456
\* generated / 14472 distinct).
SPECIFICATION Spec
INVARIANTS
    NoReachRetireFired
    NoReachQuiescentTerminate

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = TRUE
