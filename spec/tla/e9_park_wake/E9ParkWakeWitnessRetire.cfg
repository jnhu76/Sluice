\* #189 non-vacuity witness (1/2): RetireWorkerQuiescent must actually fire.
\* Expected result: VIOLATED. NoReachRetireFired is deliberately false at the
\* target; TLC's counterexample is the causal witness trace — the retire step
\* with the departure wake (wakeEpoch advances), workerAlive TRUE->FALSE, and
\* the ghost retireFired flipping (set only inside the action body, which
\* conjoins BridgeEffect).
SPECIFICATION Spec
INVARIANTS
    NoReachRetireFired

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = TRUE
