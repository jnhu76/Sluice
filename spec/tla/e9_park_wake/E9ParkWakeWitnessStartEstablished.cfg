\* R-F1 non-vacuity witness (W-START-4): the declared steady-state
\* population boundary is reached — every configured worker has published
\* startup while the rest of the state still sits at the pre-R-F1 Init
\* values (Quiescent, no participant, no parked worker, idle 0). Expected
\* result: VIOLATED. NoReachPopulationEstablished is deliberately false
\* at the target; TLC's counterexample is the projection anchor: the
\* state the S1A MODEL_SCOPE_EXCLUDED boundary took as its starting
\* point, now causally established by the StartWorker steps themselves.
SPECIFICATION Spec
INVARIANTS
    NoReachPopulationEstablished

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = TRUE
