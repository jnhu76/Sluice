\* #185 non-vacuity witness (2/3): an UN-ARMED reference park exists.
\* Expected result: VIOLATED. NoReachUnboundedRefPark is deliberately
\* false at the target; the counterexample is the witness trace — a
\* reference-config (SplitWait=FALSE) non-participant park with
\* observationArmed[w] = FALSE. This is exactly the state that must
\* NOT return without a scheduler-domain cause (InvNoCauselessReturn):
\* the reference config genuinely explores the unbounded park, it is
\* not retired from the graph, and the causeless-return detector is
\* checked against real reachable states.
SPECIFICATION Spec
INVARIANTS
    NoReachUnboundedRefPark

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = FALSE
