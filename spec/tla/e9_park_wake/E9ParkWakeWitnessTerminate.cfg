\* #189 non-vacuity witness (2/2): the terminal chain — every worker retired
\* and the invocation classified ReturnedQuiescent at true quiescence.
\* Expected result: VIOLATED. This final shape is reachable ONLY through a
\* last-alive RetireWorkerQuiescent step (ReturnQuiescent never retires a
\* worker; ParticipantNoProgressExit classifies ReturnedStalled), pinning the
\* C++-faithful terminal classification.
SPECIFICATION Spec
INVARIANTS
    NoReachQuiescentTerminate

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = TRUE
