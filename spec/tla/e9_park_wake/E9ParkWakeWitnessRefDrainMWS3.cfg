\* #185 non-vacuity witness (3/3): the scoped InvLife1DrainNoMW3Park
\* antecedent is reachable — Drain /\ MWS3 with an armed parked worker.
\* Expected result: VIOLATED. NoReachRefDrainMWS3ArmedPark is
\* deliberately false at the target; the counterexample proves the
\* post-#185 (armed-scoped) InvLife1 is non-vacuous: its antecedent is
\* reachable and its consequent (observationArmed[w] => LeaveParkEnabled)
\* is exercised in the reference config.
SPECIFICATION Spec
INVARIANTS
    NoReachRefDrainMWS3ArmedPark

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = FALSE
