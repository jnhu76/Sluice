\* R-F1 Corrective-1 non-vacuity witness (W-START-5): STARTUP-HISTORY
\* CARRY-OVER into a settled state. W1 acquires the backend participant
\* role during the startup skew (W0 configured-unstarted), W0
\* subsequently publishes startup, and W1 REMAINS the incumbent
\* participant in the settled, still-Active run. Expected result:
\* VIOLATED. NoReachStartW1IncumbentAfterSettlement is deliberately
\* false at the target; TLC's counterexample is the causal witness
\* trace proving that startup history survives into a settled state the
\* pre-R-F1 Init cannot generate (with both workers alive from Init,
\* LowestAlive = W0 always, so only W0 could ever take the participant
\* slot while W0 was alive). This pins the distinction between
\* post-settlement transition-schema correspondence and reachable-state
\* refinement. The witnessed state is LEGAL (a late starter does not
\* displace the incumbent participant; C++ election requires
\* admission_ == none, scheduler.cpp:705) — history sensitivity, not a
\* defect.
SPECIFICATION Spec
INVARIANTS
    NoReachStartW1IncumbentAfterSettlement

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = TRUE
