\* #185 non-vacuity witness (1/3): an ENTRY-ARMED reference park exists.
\* Expected result: VIOLATED. NoReachRefObservationPark is deliberately
\* false at the target; the counterexample is the witness trace — a
\* reference-config (SplitWait=FALSE) non-participant park with
\* observationArmed[w] = TRUE (the entry capture when an external
\* wake-capable wait was registered), proving the faithful
\* `~SplitWait /\ observationArmed[w]` escape is a real, exercised
\* return class (the reference MIXED-WAKE 2ms observation authority).
SPECIFICATION Spec
INVARIANTS
    NoReachRefObservationPark

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = FALSE
