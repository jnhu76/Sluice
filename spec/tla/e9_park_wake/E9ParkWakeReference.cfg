(* Phase G closeout: the NON-split (reference/legacy) backend rule —
   Fake/Sync keep the E9 bounded-observation MIXED-WAKE park (DIV-05,
   narrowed to reference backends). Safety only. *)
SPECIFICATION Spec
INVARIANTS
    Inv
    Inv2NoLostWake
    Inv4ExternalReadyWakes
    Inv6OneBackendParticipant
    Inv7MixedWakeNoBlindBackendWait
    Inv8BridgeReachesBackendPark
    Inv9NoStrandedRunnable
    InvLife1DrainNoMW3Park
    InvLife3LiveExternalParkAdmitted
    InvLife5QuiescenceClassifierDefined

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = FALSE
