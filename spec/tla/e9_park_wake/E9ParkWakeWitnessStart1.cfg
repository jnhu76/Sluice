\* R-F1 non-vacuity witness (W-START-1): W0's startup publication precedes
\* W1's. Expected result: VIOLATED. NoReachStartW0First is deliberately
\* false at the target; TLC's counterexample is the causal witness trace —
\* the StartWorker(W0) step (the run_impl thread lambda's
\* active.store(true), scheduler.cpp:460) reaching the partial-population
\* state started[W0] /\ ~started[W1] (workerStarted is monotone, so this
\* state is exactly "W0 published before W1").
SPECIFICATION Spec
INVARIANTS
    NoReachStartW0First

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = TRUE
