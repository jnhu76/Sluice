\* R-F1 non-vacuity witness (W-START-2 / W-START-3): W1's startup
\* publication precedes W0's — the historical #223/#210 shape (W0
\* configured-unstarted, W1 startup-visible). Expected result: VIOLATED.
\* NoReachStartW1First is deliberately false at the target; TLC's
\* counterexample is the causal witness trace. In the two-worker domain
\* this state is also exactly W-START-3 (a partial population: exactly
\* one configured worker startup-visible).
SPECIFICATION Spec
INVARIANTS
    NoReachStartW1First

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = TRUE
