\* #191 non-vacuity witness (1/2): ParticipantNoProgressExit must actually fire.
\* Expected result: VIOLATED. NoReachParticipantExitFired is deliberately false
\* at the target; TLC's counterexample is the causal witness trace -- the
\* participant exit step with the departure wake (wakeEpoch advances), the
\* participant slot cleared (backendWaitParticipant' = NONE), the one-shot
\* bridge consumed (bridgePending' = FALSE), workerAlive TRUE->FALSE, and the
\* ghost participantExitFired flipping (set only inside the action body,
\* which conjoins the repaired direct epoch advance).
SPECIFICATION Spec
INVARIANTS
    NoReachParticipantExitFired

CONSTANTS
    Workers = {W0, W1}
    Fibers = {F0}
    W0 = W0
    W1 = W1
    F0 = F0
    NONE = NONE
    SplitWait = TRUE
