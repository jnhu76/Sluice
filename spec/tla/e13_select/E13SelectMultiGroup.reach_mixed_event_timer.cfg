\* PR #18 corrective-1 multi-group reach witness (D2):
\* one group has an Event arm, the other an independent Timer arm; both
\* complete independently and each publishes its own result exactly once.
\* Timer arms do not occupy an Event identity, so Events = {0} suffices and
\* keeps the state space minimal.
SPECIFICATION MGSpec
INVARIANT NotMG_ReachMixedEventTimer

CONSTANTS
Groups = {0, 1}
Events = {0}
