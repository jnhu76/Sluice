\* NEG-S2: mutate claim snapshot after claim.
\* Fault_S2 removes the winner from claim_candidates post-claim while
\* claim_snapshot_frozen (the frozen-at-claim value) is left unchanged.
\* Target: S_InvClaimSnapshotImmutableAfterClaim (the strict frozen-equality
\* law that proves claim_candidates cannot be mutated after the snapshot is
\* stamped by CentralClaimWinner).
SPECIFICATION SNegSpec
INVARIANT S_InvClaimSnapshotImmutableAfterClaim
CONSTANTS
MaxArms = 2
Arms = {0, 1}
FAULT = "S2"
