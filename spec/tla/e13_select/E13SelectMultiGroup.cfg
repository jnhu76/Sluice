\* PR #18 bounded two-group composition: full multi-group safety suite.
\* Groups = {0, 1}, Events = {0}.  The single Event identity is shared across
\* both groups so the shared-Event broadcast non-interference law
\* (MG_InvEventBroadcastDeduplicatesGroupProcessing,
\*  MG_InvSharedEventDoesNotConsumeAcrossGroups) is genuinely exercised.
SPECIFICATION MGSpec
INVARIANT MGSafetyInv

CONSTANTS
Groups = {0, 1}
Events = {0}
