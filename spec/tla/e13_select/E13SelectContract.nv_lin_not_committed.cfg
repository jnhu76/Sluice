\* PR #18 corrective-1 (P1-4) Contract non-vacuity witness for the frozen
\* winner identity law: a winner has been linearized (linearized_winner_valid
\* = TRUE) but no arm has reached WinnerCommitted yet, so the identity-
\* stability law is the load-bearing guarantee in this window (not subsumed
\* by the commit law).  A TLC "Invariant NotReachContractWinnerLinearized-
\* NotCommitted is violated" on this cfg is the witness that the premise is
\* reachable in the Contract model, so C_InvWinnerIdentityStableAfter-
\* Linearization is not vacuously TRUE.
SPECIFICATION ContractSpec
INVARIANT NotReachContractWinnerLinearizedNotCommitted

CONSTANTS
MaxArms = 2
Arms = {0, 1}
