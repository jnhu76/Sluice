\* PR #18 Contract non-vacuity witness: classified loser exists (H2 premise).
\* A TLC "Invariant NotReachContractLoserExists is violated" on this cfg is the witness that the
\* premise is reachable in the Contract model, so the corresponding safety law
\* is not vacuously TRUE.
SPECIFICATION ContractSpec
INVARIANT NotReachContractLoserExists

CONSTANTS
MaxArms = 2
Arms = {0, 1}
