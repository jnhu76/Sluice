\* PR #18 Central Claim safety suite: the named layered invariant set (J)
\* over the PR #17 Central Claim strategy, on a 2-arm domain.
\* PR #17 canonical CentralInv + Contract refinement is checked separately by
\* E13SelectCentralClaim.cfg and is NOT asserted here.
SPECIFICATION CentralSpec
INVARIANT CentralSafetyInv

CONSTANTS
MaxArms = 2
Arms = {0, 1}
