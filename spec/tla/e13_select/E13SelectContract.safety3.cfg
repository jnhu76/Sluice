\* PR #18 Contract safety suite on a 3-arm domain that exercises the
\* reversible-reservation path (H5: Held -> Committed|Released, close once).
\* This is a bounded widening of the 2-arm safety cfg: more arms, more
\* reservation combinations, still the same named layered invariant set.
\* No state constraint is used; the model remains the full 3-arm Contract graph.
SPECIFICATION ContractSpec
INVARIANT ContractSafetyInv

CONSTANTS
MaxArms = 3
Arms = {0, 1, 2}
