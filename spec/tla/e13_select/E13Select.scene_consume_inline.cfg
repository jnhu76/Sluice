\* E13Select scene: inline completion -> result consumed (§K R11).
\* Proves R11 (CompletedInline -> Consumed) reachable. The full inline path:
\* registration -> admission-ready winner -> CompleteInline (runnable=0) ->
\* ConsumeResult (operation_phase = Consumed). runnable_publication_count=0
\* distinguishes the inline path from the suspended path (R12).
SPECIFICATION Spec
INVARIANT NotReach_R11

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
