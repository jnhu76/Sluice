\* E13Select scene: stale timer configuration (§L).
\* Proves R10 (stale Retired TimerRegistration skipped without dereference)
\* reachable (§K).
\*
\* The witness: a Timer arm whose registration is Retired (retired as a loser
\* via CancelTimerLoser + RetireTimerLoser: Active -> Retired, AFTER cancel/
\* detach) persists to group Completed/Destroyed. The pump's dereference gate
\* (timer_state = "Active" in TimerPumpEntry) means a Retired registration is
\* never dereferenced; TimerPumpSkip observes the stale due terminal
\* registration and sets timer_skip_observed without incrementing
\* timer_node_deref -- preparation §I / S15 / I29. NEG-T1 (the buggy variant
\* that DOES dereference) is PR #18; this scene proves the CORRECT model reaches
\* a state where a stale Retired registration coexists with a completed group.
\* R10 binds the witness to: an Event winner (arm_kind[winner] = "EventArm"),
\* a distinct Timer loser arm t with timer_state[t] = "Retired",
\* finalization_step[t] = "TimerLoser", timer_skip_observed[t], and
\* timer_node_deref[t] = 0.
SPECIFICATION Spec
INVARIANT NotReach_R10

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
