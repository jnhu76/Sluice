\* PR #18 corrective-1 multi-group non-vacuity witness:
\* the post-install pre-finish registration state is reachable (this is the
\* state from which BeginRollback is now reachable).  A TLC "Invariant
\* NotMG_ReachArmInstalledNotFinished is violated" is the witness that the
\* premise of MG_InvRollbackRequiresRegistering / RequiresInstalledArm is
\* reachable, so those rollback laws are not vacuously TRUE.
SPECIFICATION MGSpec
INVARIANT NotMG_ReachArmInstalledNotFinished

CONSTANTS
Groups = {0, 1}
Events = {0}
