\* PR #18 Central Claim -> Contract refinement on a 3-arm domain (X).
\* Same temporal PROPERTY as the PR #17 canonical CentralClaim.cfg, on a wider
\* 3-arm domain that exercises the simultaneous-offers admission tie so the
\* refinement mapping holds across every candidate combination.
SPECIFICATION CentralSpec
INVARIANT CentralInv
INVARIANT CentralSafetyInv
PROPERTY RefinesContract

CONSTANTS
MaxArms = 3
Arms = {0, 1, 2}
