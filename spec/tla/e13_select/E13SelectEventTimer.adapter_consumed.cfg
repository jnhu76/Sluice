\* PR #18 Event/Timer adapter non-vacuity witness: a Consumed timer exists (L premise).
\* A TLC "Invariant NotReachAdapterConsumedTimerExists is violated" on this cfg is the
\* witness that the premise is reachable in the adapter model, so the
\* corresponding safety law is not vacuously TRUE.
SPECIFICATION EventTimerSpec
INVARIANT NotReachAdapterConsumedTimerExists

CONSTANTS
MaxArms = 2
Arms = {0, 1}
Events = {0, 1}
