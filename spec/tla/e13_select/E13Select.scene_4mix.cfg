\* E13Select scene: 4-arm bounded configuration (§L).
\* Proves R7 (two CandidateReady arms with lowest-index tie-break) reachable
\* (§K). The 4-arm domain is rich enough that two arms can simultaneously be
\* CandidateReady (e.g. two Events both Set, or Event+Timer both due). The
\* winner is the lower index; the higher-index arm is retired Cancelled.
SPECIFICATION Spec
INVARIANT NotReach_R7

CONSTANTS
MaxArms = 4
Arms = {0, 1, 2, 3}
Events = {0, 1}
