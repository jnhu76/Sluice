\* PR #18 corrective-1 (P1-3) Central non-vacuity witness for the frozen
\* snapshot multi-candidate case: the frozen snapshot contained >= 2
\* candidates, so S_InvWinnerChosenFromSnapshot exercises a real selection
\* (not a single-element trivial case).  Requires a 2-arm cfg so that two
\* candidates can coexist at claim time.
SPECIFICATION CentralSpec
INVARIANT NotReachCentralMultiCandidateSnapshot

CONSTANTS
MaxArms = 2
Arms = {0, 1}
