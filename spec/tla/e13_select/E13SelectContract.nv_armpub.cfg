\* PR #18 Contract non-vacuity witness: arm published (H3 premise).
\* A TLC "Invariant NotReachContractArmPublished is violated" on this cfg is the witness that the
\* premise is reachable in the Contract model, so the corresponding safety law
\* is not vacuously TRUE.
SPECIFICATION ContractSpec
INVARIANT NotReachContractArmPublished

CONSTANTS
MaxArms = 2
Arms = {0, 1}
