\* Restoration: with FAULT = "None", the focused fault is never enabled, so
\* the wrapper reduces to the canonical MGSpec and the full MGSafetyInv must
\* PASS over the same two-group state space.
SPECIFICATION MGNegSpec
INVARIANT MGSafetyInv

CONSTANTS
Groups = {0, 1}
Events = {0}
FAULT = "None"
