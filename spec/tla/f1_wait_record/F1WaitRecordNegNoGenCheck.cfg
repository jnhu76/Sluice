\* NEG-WR1: the sink drops its generation comparison. The named target is
\* listed FIRST (audit 2026-08-18 convention) so TLC reports it.
SPECIFICATION Spec
INVARIANTS
    InvGenerationIsolation
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = FALSE
    CancelClearsDelivery = TRUE
    DeliveredPinBlocksReuse = TRUE
