\* BR3 reachability witness: a forged stale gen-0 event was processed
\* during epoch 1 and inertly dropped by the generation check.
SPECIFICATION Spec
INVARIANTS
    NoReachStaleDropped
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = TRUE
    CancelClearsDelivery = TRUE
    DeliveredPinBlocksReuse = TRUE
