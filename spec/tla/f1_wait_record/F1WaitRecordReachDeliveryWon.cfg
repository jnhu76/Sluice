\* BR1 both-ways coverage: a schedule where reap-extract takes the
\* delivery.
SPECIFICATION Spec
INVARIANTS
    NoReachDeliveryWon
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = TRUE
    CancelClearsDelivery = TRUE
    DeliveredPinBlocksReuse = TRUE
