\* NEG-WR3 specificity: the same mutant must PASS the unrelated laws.
\* Excluded: InvSingleAuthority (the target) and InvAuthorityPinsRecord
\* (documented co-victim: the broken arbitration lets a delivery authority
\* outlive the record's terminal retire — entailed by the same defect).
SPECIFICATION Spec
INVARIANTS
    InvGenerationIsolation
    InvSingleDelivery
    InvSlotLeasePinsRecord
    InvLiveRecordAccounting
    InvOutcomeFrozenOnPublication
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = TRUE
    CancelClearsDelivery = FALSE
    DeliveredPinBlocksReuse = TRUE
