\* NEG-WR1 specificity: the same mutant must PASS the unrelated laws.
\* Excluded (documented co-victims, semantically entailed by the defect):
\*   InvGenerationIsolation  - the target
\*   InvSlotLeasePinsRecord  - a delivered-marking under a still-registered
\*                              slot violates the slot-pin law too
\*   InvAuthorityPinsRecord  - as-built, Delivered implies the slot already
\*                              closed (cancel can no longer hold an
\*                              authority); a stale delivered-marking breaks
\*                              that coupling as well
SPECIFICATION Spec
INVARIANTS
    InvSingleAuthority
    InvSingleDelivery
    InvLiveRecordAccounting
    InvOutcomeFrozenOnPublication
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = FALSE
    CancelClearsDelivery = TRUE
    DeliveredPinBlocksReuse = TRUE
