\* BR4 witness under the NEG-WR3 mutant (CancelClearsDelivery = FALSE):
\* the SEQUENTIAL double-grant schedule - cancel granted (possibly already
\* consumed) before the delivery grant lands in the SAME epoch - must reach
\* sawDeliveryAfterCancel, proving the historical InvSingleAuthority is
\* non-vacuous against the exact sequential shape the old simultaneous-only
\* property could not see.
SPECIFICATION Spec
INVARIANT
    NoReachSequentialDoubleGrant
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = TRUE
    CancelClearsDelivery = FALSE
    DeliveredPinBlocksReuse = TRUE
