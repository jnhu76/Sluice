\* BR1 both-ways coverage: a schedule where cancel wins the arena race.
SPECIFICATION Spec
INVARIANTS
    NoReachCancelWon
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = TRUE
    CancelClearsDelivery = TRUE
    DeliveredPinBlocksReuse = TRUE
