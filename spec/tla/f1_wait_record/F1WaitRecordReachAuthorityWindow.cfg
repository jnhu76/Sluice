\* BR1 reachability witness: the NoReach* invariant is deliberately false
\* at the target state; TLC's counterexample IS the witness (an extracted
\* arena winner not yet consumed by the registry).
SPECIFICATION Spec
INVARIANTS
    NoReachAuthorityWindow
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = TRUE
    CancelClearsDelivery = TRUE
    DeliveredPinBlocksReuse = TRUE
