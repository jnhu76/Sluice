\* NEG-WR2: acquire may pop a delivered-pinned record (recycle before the
\* drain closed the lease). Named target: the P1-2 live-record accounting.
SPECIFICATION Spec
INVARIANTS
    InvLiveRecordAccounting
CONSTANTS
    Waiters = {W0, W1}
    W0 = W0
    W1 = W1
    CheckGeneration = TRUE
    CancelClearsDelivery = TRUE
    DeliveredPinBlocksReuse = FALSE
