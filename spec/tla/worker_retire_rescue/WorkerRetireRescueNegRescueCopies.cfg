\* NEG-RT2: the rescue copies instead of moves (the queue is not
\* cleared) - two live tickets for one Fiber. Named target FIRST.
SPECIFICATION Spec
INVARIANTS
    InvSingleTicket
CONSTANTS
    W0 = W0
    W1 = W1
    W0Local = W0Local
    W1Local = W1Local
    Pending = Pending
    RescueOnRetire = TRUE
    RescueMovesNotCopies = FALSE
    RerecordOwnerOnDispatch = TRUE
