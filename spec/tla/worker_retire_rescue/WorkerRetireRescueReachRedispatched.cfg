\* ER4 witness: the survivor's queue holds the ticket with the re-recorded owner after the retire.
SPECIFICATION Spec
INVARIANTS
    NoReachRedispatched
CONSTANTS
    W0 = W0
    W1 = W1
    W0Local = W0Local
    W1Local = W1Local
    Pending = Pending
    RescueOnRetire = TRUE
    RescueMovesNotCopies = TRUE
    RerecordOwnerOnDispatch = TRUE
