\* NEG-RT2 specificity: the same mutant must PASS the unrelated laws.
\* Excluded: InvSingleTicket (target), InvNoTicketOnRetiredWorker and
\* InvOwnerLocationConsistency (entailed co-victims: the duplicated
\* W0Local copy stays on the retired worker, and after a redispatch the
\* re-recorded owner no longer matches that stale copy).
SPECIFICATION Spec
INVARIANTS
    InvRunnableHasRecoverableTicket
CONSTANTS
    W0 = W0
    W1 = W1
    W0Local = W0Local
    W1Local = W1Local
    Pending = Pending
    RescueOnRetire = TRUE
    RescueMovesNotCopies = FALSE
    RerecordOwnerOnDispatch = TRUE
