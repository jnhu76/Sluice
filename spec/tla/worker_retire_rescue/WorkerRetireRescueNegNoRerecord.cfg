\* NEG-RT3: the redispatch drops the owner re-record - the stale dead
\* owner survives on the survivor's queue (the adversarial-review
\* route-to-dead-worker class). Named target FIRST.
SPECIFICATION Spec
INVARIANTS
    InvOwnerLocationConsistency
CONSTANTS
    W0 = W0
    W1 = W1
    W0Local = W0Local
    W1Local = W1Local
    Pending = Pending
    RescueOnRetire = TRUE
    RescueMovesNotCopies = TRUE
    RerecordOwnerOnDispatch = FALSE
