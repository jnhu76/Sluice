\* NEG-RT1: retire without rescue - the pre-G1 "terminate path strands
\* queued runnables" defect. Named target FIRST.
SPECIFICATION Spec
INVARIANTS
    InvNoTicketOnRetiredWorker
CONSTANTS
    W0 = W0
    W1 = W1
    W0Local = W0Local
    W1Local = W1Local
    Pending = Pending
    RescueOnRetire = FALSE
    RescueMovesNotCopies = TRUE
    RerecordOwnerOnDispatch = TRUE
