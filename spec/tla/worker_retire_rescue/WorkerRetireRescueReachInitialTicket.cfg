\* ER1 witness: an active worker owns the runnable ticket (the initial state).
SPECIFICATION Spec
INVARIANTS
    NoReachInitialTicket
CONSTANTS
    W0 = W0
    W1 = W1
    W0Local = W0Local
    W1Local = W1Local
    Pending = Pending
    RescueOnRetire = TRUE
    RescueMovesNotCopies = TRUE
    RerecordOwnerOnDispatch = TRUE
