\* ER2 witness (strengthened): W0 retired, fiber Runnable, ticket moved to the
\* pre-run domain (Pending specifically - the retire rescue itself).
SPECIFICATION Spec
INVARIANTS
    NoReachRetiredTicketPending
CONSTANTS
    W0 = W0
    W1 = W1
    W0Local = W0Local
    W1Local = W1Local
    Pending = Pending
    RescueOnRetire = TRUE
    RescueMovesNotCopies = TRUE
    RerecordOwnerOnDispatch = TRUE
