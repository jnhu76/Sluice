\* NEG-RT1 specificity: the same mutant must PASS the unrelated laws.
\* Excluded: InvNoTicketOnRetiredWorker (target) and
\* InvRunnableHasRecoverableTicket (entailed co-victim: the stranded
\* ticket is by definition unrecoverable).
SPECIFICATION Spec
INVARIANTS
    InvSingleTicket
    InvOwnerLocationConsistency
CONSTANTS
    W0 = W0
    W1 = W1
    W0Local = W0Local
    W1Local = W1Local
    Pending = Pending
    RescueOnRetire = FALSE
    RescueMovesNotCopies = TRUE
    RerecordOwnerOnDispatch = TRUE
