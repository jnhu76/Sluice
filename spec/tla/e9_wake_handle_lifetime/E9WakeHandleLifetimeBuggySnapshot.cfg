SPECIFICATION Spec
\* ONLY the named target (audit 2026-08-18): the buggy module's aggregate
\* Inv fires at the earlier bug-introduction state (SnapshotReleased) and
\* would mask the Destroyed-during-Callback lease violation the gate asserts.
INVARIANTS
    LifeInv4DestroyedNoCallback
