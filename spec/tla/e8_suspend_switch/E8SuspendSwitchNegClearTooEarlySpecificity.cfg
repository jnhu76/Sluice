\* NEG-SS3 specificity: unrelated laws hold. InvUnsavedSuspensionProtected is
\* an expected co-victim of the same defect (clearing the protection while
\* unsaved trivially breaks the authority law too), so it is excluded here by
\* design.
CONSTANTS
  GuardStealWithPending = TRUE
  RaiseBeforeVisibility = TRUE
  ClearOnlyAfterSave = FALSE

SPECIFICATION Spec

INVARIANTS
  InvTicketImpliesRunnable
  InvPendingImpliesCommitted
