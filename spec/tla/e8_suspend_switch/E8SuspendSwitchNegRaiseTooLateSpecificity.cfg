\* NEG-SS2 specificity: unrelated laws hold. InvNoResumeBeforeContextSaved is
\* an expected co-victim of the same defect chain (proven separately by the
\* chain cfg), so it is excluded here by design.
CONSTANTS
  GuardStealWithPending = TRUE
  RaiseBeforeVisibility = FALSE
  ClearOnlyAfterSave = TRUE

SPECIFICATION Spec

INVARIANTS
  InvTicketImpliesRunnable
  InvPendingImpliesCommitted
