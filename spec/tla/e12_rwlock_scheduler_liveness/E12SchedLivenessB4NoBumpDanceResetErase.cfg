\* B4 experiment: the :1065 DANCE RESET-CONTINUE idle erase (under G) has
\* its generation bump disabled (BumpDanceResetErase = FALSE). This site is
\* SELF-GUARDED (the eraser stays active and re-loops/re-dances), so
\* removing the bump must stay SAFE. Expected: all invariants PASS.
CHECK_DEADLOCK FALSE

CONSTANTS
    Workers = {W0, W1}
    Fibers = {WF, R1, R2}
    W0 = W0
    W1 = W1
    WF = WF
    R1 = R1
    R2 = R2
    NOFIBER = NOFIBER
    RepairContributionGeneration = TRUE
    BumpPopErase = TRUE
    BumpMwS1Erase = TRUE
    BumpRecheckErase = TRUE
    BumpPubErase = TRUE
    BumpDanceResetErase = FALSE
    M1NoPubSignal = FALSE
    M2NoTransport = FALSE
    M3NoCommitRecheck = FALSE
    M5GrantNoTicket = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    DrainStuckState
    NoReaderWriterOverlap
    TerminalUniqueness
    PublicationUniqueness
    NoLinkedTerminal
    InboxImpliesTicket
    NoStrandedRunnable
