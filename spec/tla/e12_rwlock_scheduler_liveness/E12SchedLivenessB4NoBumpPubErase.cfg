\* B4 experiment: the :1514 ROUTE-PUBLICATION idle erase (under G) has its
\* generation bump disabled (BumpPubErase = FALSE). RECLASSIFIED GENUINE by
\* the split-window model: with the bump off, DrainStuckState must be
\* VIOLATED (the pub site orphans a pre-grant dance contribution).
\* Expected: CEX DrainStuckState.
CHECK_DEADLOCK FALSE

CONSTANTS
    Workers = {W0, W1}
    Fibers = {WF, R1, R2}
    W0 = W0
    W1 = W1
    WF = WF
    R1 = R1
    R2 = R2
    NOFIBER = NOFIBER
    RepairContributionGeneration = TRUE
    BumpPopErase = TRUE
    BumpMwS1Erase = TRUE
    BumpRecheckErase = TRUE
    BumpPubErase = FALSE
    BumpDanceResetErase = TRUE
    M1NoPubSignal = FALSE
    M2NoTransport = FALSE
    M3NoCommitRecheck = FALSE
    M5GrantNoTicket = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    DrainStuckState
    NoReaderWriterOverlap
    TerminalUniqueness
    PublicationUniqueness
    NoLinkedTerminal
    InboxImpliesTicket
    NoStrandedRunnable
