\* Split-window reachability witness (repaired protocol, all bumps on).
\* Expected: SplitWindowNeverArmed VIOLATED — a stale contributor's park
\* commit reads the erased count together with the still-current generation
\* (the eraser's exchange(0) landed, its bump has not) and arms a baseline
\* WITHOUT the identity-term refusal. Proves the model explores the C++
\* split window; the safety gate on the SAME constants proves the window
\* costs only a transient park (DrainStuckState still holds).
CHECK_DEADLOCK FALSE

CONSTANTS
    Workers = {W0, W1}
    Fibers = {WF, R1, R2}
    W0 = W0
    W1 = W1
    WF = WF
    R1 = R1
    R2 = R2
    NOFIBER = NOFIBER
    RepairContributionGeneration = TRUE
    BumpPopErase = TRUE
    BumpMwS1Erase = TRUE
    BumpRecheckErase = TRUE
    BumpPubErase = TRUE
    BumpDanceResetErase = TRUE
    M1NoPubSignal = FALSE
    M2NoTransport = FALSE
    M3NoCommitRecheck = FALSE
    M5GrantNoTicket = FALSE

SPECIFICATION Spec

INVARIANT
    SplitWindowNeverArmed
