\* B4 experiment: the :958 DANCE-BLOCK RECHECK idle erase (under G) has its
\* generation bump disabled (BumpRecheckErase = FALSE). This site is
\* SELF-GUARDED (the :582 fall-through always precedes it on the same path),
\* so removing the bump must stay SAFE. Expected: all invariants PASS.
CHECK_DEADLOCK FALSE

CONSTANTS
    Workers = {W0, W1}
    Fibers = {WF, R1, R2}
    W0 = W0
    W1 = W1
    WF = WF
    R1 = R1
    R2 = R2
    NOFIBER = NOFIBER
    RepairContributionGeneration = TRUE
    BumpPopErase = TRUE
    BumpMwS1Erase = TRUE
    BumpRecheckErase = FALSE
    BumpPubErase = TRUE
    BumpDanceResetErase = TRUE
    M1NoPubSignal = FALSE
    M2NoTransport = FALSE
    M3NoCommitRecheck = FALSE
    M5GrantNoTicket = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    DrainStuckState
    NoReaderWriterOverlap
    TerminalUniqueness
    PublicationUniqueness
    NoLinkedTerminal
    InboxImpliesTicket
    NoStrandedRunnable
