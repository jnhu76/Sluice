\* Mutant M5 (single defect on the REPAIRED base). See the module header.
\* Property-SPECIFIC liveness: the cfg lists ONLY
\* CancelUnlockScenarioEventuallyCompletes (the grant publishes no runnable
\* ticket, so the scenario never completes — run returns STALLED with
\* r2Acquired false), so a TLC temporal-violation verdict names exactly that
\* property. Expected: CEX CancelUnlockScenarioEventuallyCompletes.
CHECK_DEADLOCK FALSE

CONSTANTS
    Workers = {W0, W1}
    Fibers = {WF, R1, R2}
    W0 = W0
    W1 = W1
    WF = WF
    R1 = R1
    R2 = R2
    NOFIBER = NOFIBER
    RepairContributionGeneration = TRUE
    BumpPopErase = TRUE
    BumpMwS1Erase = TRUE
    BumpRecheckErase = TRUE
    BumpPubErase = TRUE
    BumpDanceResetErase = TRUE
    M1NoPubSignal = FALSE
    M2NoTransport = FALSE
    M3NoCommitRecheck = FALSE
    M5GrantNoTicket = TRUE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    DrainStuckState
    NoReaderWriterOverlap
    TerminalUniqueness
    PublicationUniqueness
    NoLinkedTerminal
    InboxImpliesTicket
    NoStrandedRunnable

PROPERTY
    CancelUnlockScenarioEventuallyCompletes
