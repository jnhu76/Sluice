\* Positive liveness gate: the contribution-generation repair, at the EXACT
\* as-built repaired constants (the three genuine invalidation sites bump;
\* the :958 recheck and :1065 reset-continue sites do NOT bump — C++ uses
\* plain store(0) there). Expected: all temporal properties PASS (fairness
\* only on scheduler-controlled actions).
CHECK_DEADLOCK FALSE

CONSTANTS
    Workers = {W0, W1}
    Fibers = {WF, R1, R2}
    W0 = W0
    W1 = W1
    WF = WF
    R1 = R1
    R2 = R2
    NOFIBER = NOFIBER
    RepairContributionGeneration = TRUE
    BumpPopErase = TRUE
    BumpMwS1Erase = TRUE
    BumpRecheckErase = FALSE
    BumpPubErase = TRUE
    BumpDanceResetErase = FALSE
    M1NoPubSignal = FALSE
    M2NoTransport = FALSE
    M3NoCommitRecheck = FALSE
    M5GrantNoTicket = FALSE

SPECIFICATION Spec

PROPERTY
    DrainEventuallyReturns
    CancelUnlockScenarioEventuallyCompletes
    GrantedWaiterEventuallyResumes
    RunnableEventuallyExecutes
