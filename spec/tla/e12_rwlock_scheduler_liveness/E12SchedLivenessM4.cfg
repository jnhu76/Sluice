\* M4 = the as-built protocol (RepairContributionGeneration = FALSE).
\* Expected: DrainStuckState VIOLATED — the counterexample must realize
\* PopTicket -> EraseIdle -> peer DanceContribute(not-last) -> stale
\* contributor ParkCommit -> ArmBaseline -> both Parked (issue #161 B2).
CHECK_DEADLOCK FALSE

CONSTANTS
    Workers = {W0, W1}
    Fibers = {WF, R1, R2}
    W0 = W0
    W1 = W1
    WF = WF
    R1 = R1
    R2 = R2
    NOFIBER = NOFIBER
    RepairContributionGeneration = FALSE
    BumpPopErase = TRUE
    BumpMwS1Erase = TRUE
    BumpRecheckErase = TRUE
    BumpPubErase = TRUE
    BumpDanceResetErase = TRUE
    M1NoPubSignal = FALSE
    M2NoTransport = FALSE
    M3NoCommitRecheck = FALSE
    M5GrantNoTicket = FALSE

SPECIFICATION Spec

INVARIANTS
    DrainStuckState
    NoReaderWriterOverlap
    TerminalUniqueness
    PublicationUniqueness
    NoLinkedTerminal
    InboxImpliesTicket
    NoStrandedRunnable
