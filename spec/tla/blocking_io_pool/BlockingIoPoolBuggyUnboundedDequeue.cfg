\* NEG-BIP-1 (Dequeue drops the in-flight worker bound): TLC must VIOLATE the named invariant.
CHECK_DEADLOCK FALSE

CONSTANTS
    NumWorkers = 2
    MaxDepth = 2
    NumTasks = 3

INVARIANT WorkerBoundInvariant

SPECIFICATION Spec
