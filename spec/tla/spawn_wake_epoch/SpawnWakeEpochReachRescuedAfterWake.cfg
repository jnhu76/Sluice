\* DR4 (strong form) witness: the full POST-PARK rescue chain - the cv
\* predicate fired on a committed park, then the ticket was stolen and
\* consumed. The weaker NoReachRescued is also kept: it covers the
\* never-parked delivery chain.
SPECIFICATION Spec
INVARIANTS
    NoReachRescuedAfterWake
CONSTANTS
    SignalOnPublication = TRUE
    RecheckEnforced = TRUE
