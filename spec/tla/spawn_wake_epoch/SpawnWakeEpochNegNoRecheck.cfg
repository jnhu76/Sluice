\* NEG-SP4: the exact pre-G1 consumed-baseline commit (the baseline is
\* armed regardless of a live stealable ticket). Named target FIRST.
SPECIFICATION Spec
INVARIANTS
    InvWakeObligation
CONSTANTS
    SignalOnPublication = TRUE
    RecheckEnforced = FALSE
