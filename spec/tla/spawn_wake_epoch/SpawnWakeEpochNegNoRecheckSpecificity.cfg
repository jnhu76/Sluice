\* NEG-SP4 specificity: the same mutant must PASS the unrelated laws.
\* Excluded: InvWakeObligation (the target) and InvStealRequiresAwake
\* (documented co-victim: the commit recheck is exactly what keeps a
\* worker from parking on top of its own stolen ticket - the pre-G1
\* shape; entailed by the same defect).
SPECIFICATION Spec
INVARIANTS
    InvBaselineSound
    InvConsumedRequiresPublication
CONSTANTS
    SignalOnPublication = TRUE
    RecheckEnforced = FALSE
