\* RequestArena positive safety gate (FAULT = "None" doubles as the restore
\* gate for every fault cfg: with faults disabled the full invariant
\* conjunction re-passes).
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT Inv

CONSTANTS
MaxGen = 2
FAULT = "None"
