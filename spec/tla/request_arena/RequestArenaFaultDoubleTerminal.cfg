\* NEG-RA-1: record_terminal ignores the exactly-once check; the second
\* winner write must violate the named per-generation winner bound.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT InvNoDoubleTerminal

CONSTANTS
MaxGen = 2
FAULT = "DoubleTerminal"
