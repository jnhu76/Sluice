\* W4: slot reused by a second committed occupant — stale-key window live
\* (expect VIOLATED = reachable).
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT NotReach_W4_ReusedSlotCommitted

CONSTANTS
MaxGen = 2
FAULT = "None"
