\* NEG-RA-3: a worker path publishes Completion-ready directly, bypassing the
\* reap handshake (registration close, borrow end, accounting).
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT InvPublishedCompleteness

CONSTANTS
MaxGen = 2
FAULT = "DirectPublish"
