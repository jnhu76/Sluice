\* NEG-RA-6: running cancel stores the canceled terminal directly instead of
\* recording intent (Decision 11 verbatim violation).
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT InvCanceledTerminalSource

CONSTANTS
MaxGen = 2
FAULT = "RunningCancelStores"
