\* RequestArena liveness gate: WF(Enqueue) + WF(Reap) only (see the fairness
\* note in the module — backend/syscall termination is an environment
\* assumption, NOT a fairness clause here).
SPECIFICATION LivenessSpec
CHECK_DEADLOCK FALSE
PROPERTY LivenessProps

CONSTANTS
MaxGen = 2
FAULT = "None"
