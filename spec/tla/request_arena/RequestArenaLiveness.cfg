\* RequestArena liveness gate: WF(Enqueue) + WF(Reap) are LAYER-B external
\* progress obligations (backend submit path; backend/runtime progress loop)
\* — the arena leaf guarantees neither. The properties are CONDITIONAL on
\* those obligations; backend/syscall termination remains an environment
\* assumption with no fairness clause here. CHECK_DEADLOCK FALSE: terminal
\* states legitimately have no enabled action; deadlock-freedom is not a
\* leaf-model claim (see the Liveness note in the module).
SPECIFICATION LivenessSpec
CHECK_DEADLOCK FALSE
PROPERTY LivenessProps

CONSTANTS
MaxGen = 2
FAULT = "None"
