\* NEG-RA-4: release skips the generation increment; the freed occupant's
\* stale key would match the next occupant (I6 violation).
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT InvGenAdvanceOnFree

CONSTANTS
MaxGen = 2
FAULT = "NoGenIncrement"
