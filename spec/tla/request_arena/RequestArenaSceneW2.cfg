\* W2: enqueue observed the canceled terminal and acked the pin
\* (successful terminal_noop) (expect VIOLATED = reachable).
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT NotReach_W2_EnqueueNoopAfterCancel

CONSTANTS
MaxGen = 2
FAULT = "None"
