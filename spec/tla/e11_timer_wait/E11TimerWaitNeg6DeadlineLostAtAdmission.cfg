\* E11 NEG-6 negative-model TLC config.
\* Broken: the final admission decision commits suspension even when the deadline
\* is already due -> the wait is parked on an already-due deadline (I5 violation).
\* Expected: InvDeadlineAdmissionClosure FAIL.
SPECIFICATION Spec
INVARIANT InvDeadlineAdmissionClosure

CONSTANTS
N0 = N0
N1 = N1
Nodes = {N0, N1}
R0 = R0
R1 = R1
Regs = {R0, R1}
