\* CR2 reachability witness: a consumer actually delivered a request.
SPECIFICATION Spec
INVARIANTS
    NoReachDelivered
CONSTANTS
    Consumers = {A, B}
    A = A
    B = B
    AckIsEpochRelative = TRUE
    ClearClearsPending = TRUE
    SingleShot = TRUE
    CheckPending = TRUE
    ProtectionBlocks = TRUE
