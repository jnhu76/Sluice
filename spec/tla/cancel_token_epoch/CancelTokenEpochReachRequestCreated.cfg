\* CR1 reachability witness: a request was created (pending with epoch >= 1).
SPECIFICATION Spec
INVARIANTS
    NoReachRequestCreated
CONSTANTS
    Consumers = {A, B}
    A = A
    B = B
    AckIsSticky = TRUE
    ClearClearsPending = TRUE
    SingleShot = TRUE
    CheckPending = TRUE
    ProtectionBlocks = TRUE
