\* NEG-CT1: the acknowledgement is a sticky bool (the pre-fix representation,
\* ADR-cancel-request-epoch §1 finding 2) - ack never becomes request-relative.
\* AckIsEpochRelative = FALSE selects the mutant (TRUE = as-built).
\* Named target listed FIRST (audit 2026-08-18 convention).
SPECIFICATION Spec
INVARIANTS
    InvAckIsRealEpoch
CONSTANTS
    Consumers = {A, B}
    A = A
    B = B
    AckIsEpochRelative = FALSE
    ClearClearsPending = TRUE
    SingleShot = TRUE
    CheckPending = TRUE
    ProtectionBlocks = TRUE
