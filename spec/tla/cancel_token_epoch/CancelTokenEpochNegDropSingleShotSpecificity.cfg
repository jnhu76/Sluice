\* NEG-CT3 specificity: the drop-single-shot mutant must PASS the unrelated
\* laws. Excluded: InvSingleShotPerEpoch (the named target) only - the
\* pending gate, protection gate and epoch-relative ack update are intact, so
\* provenance, protection, clear, starve and ack-representation all hold.
SPECIFICATION Spec
INVARIANTS
    InvNoDeliveryWhenIdle
    InvProtectionBlocksDelivery
    InvClearRemovesIntent
    InvNoStaleAckStarvesDelivery
    InvDeliveredWasRequested
    InvAckIsRealEpoch
CONSTANTS
    Consumers = {A, B}
    A = A
    B = B
    AckIsEpochRelative = TRUE
    ClearClearsPending = TRUE
    SingleShot = FALSE
    CheckPending = TRUE
    ProtectionBlocks = TRUE
