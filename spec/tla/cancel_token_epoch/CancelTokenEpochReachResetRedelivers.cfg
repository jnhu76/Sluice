\* Per-consumer re-arm witness: a consumer that already delivered the CURRENT
\* request delivers it again after CancelState::reset_acknowledgement() (ADR
\* semantics table; tests/cancel_token_test.cpp T-CANCEL-SHARED-4) - the
\* reachable positive shape of "single-shot between explicit re-arms".
SPECIFICATION Spec
INVARIANTS
    NoReachResetRedelivers
CONSTANTS
    Consumers = {A, B}
    A = A
    B = B
    AckIsEpochRelative = TRUE
    ClearClearsPending = TRUE
    SingleShot = TRUE
    CheckPending = TRUE
    ProtectionBlocks = TRUE
