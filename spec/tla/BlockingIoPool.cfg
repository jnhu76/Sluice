\* TLC model configuration for BlockingIoPool (sluice-CORE-024S).
\* Run: java -cp /tmp/tla2tools.jar tlc2.TLC -config BlockingIoPool.cfg BlockingIoPool.tla
\*
\* Terminal quiescence is checked explicitly by NoInternalProtocolStuck. Do not
\* treat TLC's built-in terminal-state deadlock check as a protocol proof.
CHECK_DEADLOCK FALSE

CONSTANTS
    NumWorkers = 2
    MaxDepth = 2
    NumTasks = 3

INVARIANTS
    QueueBoundInvariant
    StateConsistency
    TaskLifecycle
    Linearizable
    NoInternalProtocolStuck

SPECIFICATION Spec
