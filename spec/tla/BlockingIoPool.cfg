\* TLC model configuration for BlockingIoPool (sluice-CORE-024S).
\* Run:  java -cp /tmp/tla2tools.jar tlc2.TLC -config BlockingIoPool.cfg BlockingIoPool

CONSTANTS
    NumWorkers = 2
    MaxDepth = 2
    NumTasks = 3

INVARIANTS
    QueueBoundInvariant
    StateConsistency
    TaskLifecycle
    Linearizable

SPECIFICATION Spec
